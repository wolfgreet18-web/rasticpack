# نقشه راه معماری — مقیاس‌پذیری اپ به بی‌نهایت رکورد (مشتری/فاکتور) بدون افت کارایی

**نسخه سند:** ۴.۸
**هدف:** انتقال معماری فعلی (آرایه‌ی درون‌حافظه + IndexedDB ساده) به یک معماری native-grade که میلیون‌ها رکورد را بدون کرش، فریز، یا لگ مدیریت کند.

> **یادداشت نسخه ۴.۸ (این نسخه):** طبق نقشه (بخش ۶، ردیف ریسک «حجم فایل دیتابیس روی گوشی‌های حافظه‌محدود مشکل ایجاد کند» — بند باز باقی‌مانده از ۴.۷: «تصمیم صریح درباره‌ی نیاز به VACUUM دوره‌ای که فعلاً در هیچ فازی برنامه‌ریزی نشده»)، قدم بعدی مشخص و قابل‌اجرا در این محیط (بدون مرورگر/Android/شبکه) این بود: خودِ آن سؤال باز را با عدد واقعی جواب بدهیم — نه فقط insert خالص (که ۴.۷ اندازه گرفت)، بلکه چرخه‌ی واقعی **حذف+درج (churn)**، چون یک دیتابیس insert-only خالص اصلاً به VACUUM نیاز ندارد؛ مسئله‌ی صفحات آزادِ بازیافت‌نشده فقط وقتی رخ می‌دهد که حذف هم در کار باشد (دقیقاً همان چیزی که ۴.۷ به‌عنوان محدودیت اندازه‌گیری‌نشده ثبت کرده بود). این کار انجام شد:
> - فایل جدید `tests/vacuum-churn-test.mjs` نوشته شد: دو دیتابیس فایل‌محور واقعی (نه `:memory:`) جدا از هم روی دیسک ساخته می‌شوند، هرکدام با همان schema کامل و همان seed پایه (۱۵٬۰۰۰ مشتری + ۵۰۰٬۰۰۰ فاکتور، steady-state نه جدول خالی). سپس روی هرکدام ۱۰ دور churn اجرا می‌شود — در هر دور، ۵٪ از فاکتورهای موجود (id تصادفی پراکنده در کل جدول، نه فقط انتهای آن) حذف و همان تعداد فاکتور جدید insert می‌شود، طوری که تعداد کل ردیف‌ها همیشه ۵۰۰k باقی می‌ماند و فقط جابه‌جایی داخلی رخ می‌دهد. در سناریوی اول هیچ‌وقت `VACUUM` اجرا نمی‌شود؛ در سناریوی دوم بعد از هر دور churn یک `VACUUM` اجرا و زمانش با `process.hrtime.bigint()` جمع زده می‌شود.
> - نتیجه (یک اجرا — مثل ۴.۷، این یک اندازه‌گیری دیسک‌محور قطعی روی یک سناریوی مشخص است، نه نمونه‌گیری آماری):
>
>   | سناریو | حجم پایه (۵۰۰k، قبل از churn) | حجم نهایی (بعد از ۱۰ دور churn) | نسبت رشد | زمان کل صرف‌شده در VACUUM |
>   |---|---|---|---|---|
>   | بدون VACUUM | ۱۵۱.۱۸ MB | ۱۸۷.۹۶ MB | ۱.۲۴x | — |
>   | با VACUUM بعد از هر دور | ۱۵۱.۱۸ MB | ۱۴۱.۰۶ MB | ۰.۹۳x (کوچک‌تر از پایه) | ۱۷۸۲۰ms (میانگین ۱۷۸۲ms/دور) |
>
>   تعداد ردیف نهایی هر دو سناریو دقیقاً یکسان است (۵۰۰٬۰۰۰ فاکتور) — یعنی تفاوت ۴۶.۹۱ مگابایتی بین دو سناریو صرفاً از صفحات آزادِ بازیافت‌نشده است، نه داده‌ی واقعی بیشتر. جالب توجه: سناریوی «با VACUUM» نه‌فقط بادنکرده، بلکه از حجم پایه هم کمی کوچک‌تر تمام شد (۰.۹۳x) — چون VACUUM هر بار fragmentation ایجادشده توسط چرخه‌ی قبلی را هم جمع می‌کند، نه فقط جلوی رشد جدید را می‌گیرد.
> - ۱۸ assertion موجود در `run-integration-tests.mjs` دوباره اجرا شد — همه پاس شدند، بدون رگرسیون (این تست فقط یک فایل benchmark جدید اضافه کرد، هیچ فایل production را تغییر نداد).
> - **نتیجه‌گیری برای این بند از ردیف ریسک:** با این سناریو (۵٪ churn در هر دور، ۱۰ دور، پراکندگی تصادفی id)، عدم اجرای VACUUM باعث ~۲۴٪ حجم اضافه نسبت به حالت بدون churn می‌شود — قابل‌توجه ولی نه فاجعه‌بار در کوتاه‌مدت؛ اما این عدد **تجمعی و پیش‌رونده** است (هر دور churn بیشتر، بدون VACUUM، رشد را ادامه می‌دهد، چون SQLite صفحات آزاد را برای استفاده‌ی مجدد نگه می‌دارد ولی هرگز به فایل‌سیستم برنمی‌گرداند مگر VACUUM دستی). در طرف مقابل، هزینه‌ی VACUUM ناچیز نیست: ~۱.۸ ثانیه به‌ازای هر دور روی ۵۰۰k رکورد، و مهم‌تر، **VACUUM دیتابیس را حین اجرا قفل می‌کند** — یعنی اجرای آن بعد از *هر* دور churn (مثلاً بعد از هر عملیات حذف/درج تکی در یک اپ واقعی) عملاً غیرقابل‌قبول است؛ این باید به‌صورت یک job زمان‌بندی‌شده‌ی کم‌تکرار (مثلاً شبانه یا هفتگی، نه بعد از هر عملیات) طراحی شود.
> - **مهم:** این هم مثل بقیه‌ی موارد فاز ۱، عدد `node:sqlite` روی فایل‌سیستم/CPU کانتینر توسعه است، نه دستگاه اندروید/iOS واقعی از طریق پلاگین `@capacitor-community/sqlite` — نسبت رشد (۱.۲۴x) به فرمت استاندارد فایل SQLite3 وابسته است و باید در همان مرتبه‌ی بزرگی روی دستگاه واقعی هم معتبر بماند، ولی زمان اجرای VACUUM (۱۷۸۲ms/دور) کاملاً به سرعت CPU/IO دستگاه وابسته است و روی گوشی‌های ضعیف‌تر می‌تواند به‌طور محسوس کندتر باشد — این عدد فقط نسبت هزینه/فایده را نشان می‌دهد، نه زمان دقیق قابل‌انتظار روی موبایل. این ردیف ریسک را از «🟡 عدد اولیه‌ی insert-only» به «۲ عدد اضافه‌شده: bloat واقعی ناشی از churn + هزینه‌ی زمانی VACUUM — هنوز تصمیم نهایی نگرفته شده، ولی اکنون داده‌محور» تغییر می‌دهد؛ توصیه‌ی عملی (نه تصمیم نهایی، چون همچنان به تأیید دستگاه واقعی نیاز دارد): VACUUM را به‌عنوان یک maintenance job کم‌تکرار (نه synchronous بعد از هر تراکنش) در فاز ۶ (Backup/Restore) یا یک فاز نگهداری جداگانه در نظر بگیرید، نه در مسیر بحرانی نوشتن. تصمیم نهایی همچنان به آیتم ۱ از «باقی‌مانده‌های فاز ۱» (اتصال به دستگاه واقعی) موکول است.
>
> **یادداشت نسخه ۴.۷ (نسخه‌ی قبل): طبق نقشه (بخش ۶، ردیف ریسک «حجم فایل دیتابیس روی گوشی‌های حافظه‌محدود مشکل ایجاد کند» — راهکار: «تعیین سقف واقعی بر اساس فضای ذخیره‌سازی هدف در فاز ۰»، وضعیت قبلی: ⏳ فاز ۰ هیچ عدد baseline ثبت نکرده بود)، قدم بعدی مشخص و قابل‌اجرا در این محیط (بدون مرورگر/Android/شبکه) این بود: برخلاف تمام تست‌های قبلی این پروژه (که همه از `:memory:` استفاده می‌کردند)، یک دیتابیس **فایل‌محور واقعی روی دیسک** بسازیم و حجم واقعی فایل `.db` را در چند نقطه‌ی رشد اندازه بگیریم — نه فقط زمان اجرای query را. این کار انجام شد:
> - فایل جدید `tests/db-file-size-test.mjs` نوشته شد: یک `DatabaseSync` با مسیر فایل واقعی (نه `:memory:`) در `os.tmpdir()` ساخته می‌شود، با همان schema کامل (۶ ایندکس روی `invoices`، شامل ایندکس پوششی ۴.۵) که در `perf-test-1m.mjs` استفاده می‌شود. همان ۱۵٬۰۰۰ مشتری seed می‌شود، و فاکتورها در سه نقطه‌ی تجمعی (۱۰۰k / ۵۰۰k / ۱M) نوشته می‌شوند؛ بعد از هر نقطه، حجم فایل هم قبل و هم بعد از `VACUUM` با `fs.statSync` اندازه‌گیری می‌شود (بدون هیچ override دستی روی `page_size`/`journal_mode` — دقیقاً پیش‌فرض‌های خودِ SQLite، هم‌راستا با این‌که `connection.js` پروژه هم چنین override‌ای ندارد).
> - نتیجه (یک اجرا — برخلاف تست‌های زمان‌سنجی قبلی، این یک اندازه‌گیری دیسک‌محور قطعی است، نه یک نمونه‌گیری آماری که نیاز به تکرار برای واریانس داشته باشد):
>
>   | رکوردها (فاکتور) | حجم فایل خام (بعد از COMMIT) | حجم فایل بعد از VACUUM | بایت به‌ازای هر فاکتور (بعد از VACUUM) |
>   |---|---|---|---|
>   | ۱۰۰,۰۰۰ | ۳۱.۲۷ MB | ۲۹.۴۷ MB | ~۳۰۹ بایت |
>   | ۵۰۰,۰۰۰ | ۱۵۰.۸۰ MB | ۱۴۰.۹۷ MB | ~۲۹۶ بایت |
>   | ۱,۰۰۰,۰۰۰ | ۳۰۰.۸۲ MB | ۲۸۰.۳۳ MB | ~۲۹۴ بایت |
>
>   یعنی رشد تقریباً **خطی** است (نه نمایی) — حدود ۲۹۴ بایت به‌ازای هر فاکتور در مقیاس بزرگ (۱۵٬۰۰۰ مشتری خودشان فقط چند صد کیلوبایت‌اند و در این عدد گم می‌شوند)، و فایل نهایی برای معیار پذیرش رسمی فاز ۱ (۱M فاکتور) حدود **۲۸۰ مگابایت** است. برون‌یابی خطی (نه اندازه‌گیری‌شده، فقط تخمین) برای ۱۰M فاکتور حدود ۲.۷ گیگابایت می‌شود.
> - ۱۸ assertion موجود در `run-integration-tests.mjs` دوباره اجرا شد — همه پاس شدند، بدون رگرسیون (این تست فقط یک فایل benchmark جدید اضافه کرد، هیچ فایل production را تغییر نداد).
> - **نتیجه‌گیری برای این ردیف ریسک:** این اولین عدد واقعی (نه حدسی) برای حجم فایل دیتابیس در مقیاس هدف رسمی پروژه است. ۲۸۰ مگابایت برای گوشی‌های امروزی (حتی رده‌پایین با ۳۲–۶۴ گیگابایت حافظه‌ی داخلی) به‌تنهایی بحرانی نیست، ولی سه نکته باید صادقانه ثبت شود: (۱) این عدد شامل حجم اپلیکیشن/APK/کش مرورگر/سایر داده‌های اپ نیست، فقط خودِ فایل SQLite؛ (۲) داده‌ی تستی این آزمایش ساده‌شده است — ستون `data` (JSON شامل `items[]`) در این seed فقط یک آیتم دارد، در حالی که فاکتورهای واقعی می‌توانند `items[]` طولانی‌تری داشته باشند و حجم واقعی را بالاتر ببرند؛ (۳) بدون `VACUUM` دوره‌ای (که پروژه فعلاً هیچ‌جا برنامه‌ریزی نکرده، نه در فاز ۱ و نه بعدش)، فایل واقعی به‌مرور از این عدد بزرگ‌تر می‌ماند چون صفحات حذف‌شده توسط SQLite بازیافت نمی‌شوند مگر VACUUM دستی اجرا شود.
> - **مهم:** این هم مثل بقیه‌ی موارد فاز ۱، عدد `node:sqlite` روی فایل‌سیستم کانتینر توسعه است، نه فایل‌سیستم واقعی یک دستگاه اندروید/iOS از طریق پلاگین `@capacitor-community/sqlite` — `page_size` پیش‌فرض و رفتار دقیق VACUUM بین این دو می‌تواند کمی فرق کند، ولی چون فرمت فایل SQLite3 استاندارد است، عدد باید در همان مرتبه‌ی بزرگی معتبر بماند. این ردیف ریسک را از «⏳ بدون baseline» به «عدد اولیه‌ی اندازه‌گیری‌شده در سطح فایل SQLite — نه تأییدشده روی دستگاه واقعی» تغییر می‌دهد؛ تصمیم نهایی درباره‌ی نیاز به VACUUM دوره‌ای یا محدودیت سقف داده هنوز باز است و به آیتم ۱ از «باقی‌مانده‌های فاز ۱» (اتصال به دستگاه واقعی) موکول می‌ماند.
>
> **یادداشت نسخه ۴.۶ (نسخه‌ی قبل): طبق نقشه (بخش ۶، ردیف ریسک ایندکس پوششی — بند باز ⓑ: «هزینه‌ی نوشتن تکی با ایندکس سوم اضافه هنوز جداگانه اندازه‌گیری نشده»)، قدم بعدی مشخص و قابل‌اجرا در این محیط (بدون مرورگر/Android/شبکه) این بود: همان شکاف را ببندیم — هزینه‌ی خالص `idx_inv_date_status_sent` (ایندکس سوم روی `invoices`، اضافه‌شده در ۴.۵) روی یک `INSERT`/`UPDATE` تکی (نه bulk-insert) را مستقیماً اندازه بگیریم. این کار انجام شد:
> - فایل جدید `tests/single-write-cost-test.mjs` نوشته شد: دو دیتابیس `node:sqlite` در حافظه ساخته می‌شود — یکی با schema فعلی (۶ ایندکس روی `invoices`)، دیگری با همان schema منهای فقط `idx_inv_date_status_sent` (معادل دقیق نسخه‌ی ۴.۴). هر دو با ۱۵٬۰۰۰ مشتری و ۱M فاکتور seed می‌شوند (steady-state، نه جدول خالی)، سپس ۲۰۰۰ `INSERT` تکی و ۲۰۰۰ `UPDATE` تکی جداگانه (نه در یک تراکنش bulk) روی هرکدام با `process.hrtime.bigint()` اندازه‌گیری و مقایسه می‌شود.
> - نتیجه (۳ اجرای مستقل برای بررسی واریانس، مثل روش ۴.۴/۴.۵):
>   - هزینه‌ی خالص اضافه‌شده به هر `INSERT` تکی: **~۰.۰۱–۰.۰۲ms** (میانگین با ایندکس سوم ۰.۱۰۵–۰.۱۲۵ms در برابر ۰.۱۰۲–۰.۱۰۶ms بدون آن) — از نظر نسبی ۱۳٪ تا ۲۳٪ به نظر می‌رسد چون خودِ عدد پایه‌ بسیار کوچک است، ولی از نظر مطلق ناچیز.
>   - هزینه‌ی خالص اضافه‌شده به هر `UPDATE` تکی (که فقط `status`/`sent`/`editedAt` را تغییر می‌دهد، یعنی دقیقاً ستون‌هایی که ایندکس جدید را هم لمس می‌کنند): **~۰.۰۱–۰.۰۱۳ms** میانگین (نسبی ۱۳٪ تا ۳۳٪، مطلق باز هم ناچیز).
>   - p99 هر دو عملیات حدود **۰.۱–۰.۲۱ms** بیشتر شد — یعنی حتی در بدترین ۱٪ موارد (که احتمالاً به fsync/checkpoint دیسک مربوط است، نه خودِ ایندکس)، افزایش هنوز به مرتبه‌ی میلی‌ثانیه‌ی کامل نمی‌رسد.
> - ۱۸ assertion موجود در `run-integration-tests.mjs` دوباره اجرا شد — همه پاس شدند، بدون رگرسیون (این تست فقط یک فایل benchmark جدید اضافه کرد، هیچ فایل production را تغییر نداد).
> - **نتیجه‌گیری برای بند ⓑ:** با این حجم seed (۱M ردیف) و این تعداد نمونه (۲۰۰۰ تکرار)، هزینه‌ی نوشتن تکی ایندکس سوم در محدوده‌ی زیر-میلی‌ثانیه است و در برابر آستانه‌های پذیرش این پروژه (۵۰ms برای خواندن) عملاً بی‌اثر است — این ریسک را از «اندازه‌گیری‌نشده» به «اندازه‌گیری‌شده و بی‌خطر (در سطح SQL)» تغییر می‌دهد.
> - **مهم:** این هم مثل بند ⓐ همان ردیف ریسک (که همچنان باز می‌ماند)، عدد `node:sqlite` روی CPU کانتینر توسعه است، نه پلاگین `@capacitor-community/sqlite` با IPC واقعی روی دستگاه — روی دستگاه واقعی، سربار IPC هر عملیات نوشتن را (صرف‌نظر از تعداد ایندکس) به‌طور قابل‌توجهی بالاتر می‌برد، و این آزمایش آن لایه را اندازه نمی‌گیرد. این آزمایش فقط نشان می‌دهد که *خودِ ایندکس اضافه* در سطح SQL/storage-engine هزینه‌ی قابل‌توجهی ندارد؛ نتیجه‌گیری درباره‌ی هزینه‌ی کامل نوشتن روی موبایل واقعی همچنان منتظر آیتم ۱ از «باقی‌مانده‌های فاز ۱» (اتصال به دستگاه واقعی) است.
>
> **یادداشت نسخه ۴.۵ (نسخه‌ی قبل): طبق نقشه (بخش ۶، جدول ریسک‌ها — ریسکی که خودِ نسخه‌ی ۴.۴ کشف کرده بود)، قدم بعدی مشخص و قابل‌اجرا در این محیط (بدون مرورگر/Android/شبکه) این بود: راهکار پیشنهادی همان ریسک — «ایندکس پوششی (covering index) روی `(date, status, sent)`» — را واقعاً پیاده‌سازی و با تکرار `perf-test-1m.mjs` تأیید کنیم. این کار انجام شد:
> - ایندکس جدید `idx_inv_date_status_sent ON invoices(date, status, sent)` به `SCHEMA_STATEMENTS` در `src/db/schema.js` اضافه شد (و نسخه‌ی آینه‌ای آن در `tests/_node_test_copies/schema.js` هم‌گام‌سازی شد) — چون `getStatusCounts` وقتی فقط بازه‌ی تاریخ فیلتر می‌شود (بدون `customerId`) فقط به ستون‌های `date`/`status`/`sent` نیاز دارد، این ایندکس به SQLite اجازه‌ی یک index-only scan می‌دهد، بدون رجوع به ردیف اصلی جدول برای هر رکورد.
> - کل مجموعه تست (۱۸ assertion در `run-integration-tests.mjs`) دوباره اجرا شد — همه پاس شدند، بدون رگرسیون (ایندکس جدید فقط اضافه شده، هیچ رفتار/schema موجودی تغییر نکرده).
> - `perf-test-1m.mjs` سه بار مستقل دوباره اجرا شد (همان روش نسخه‌ی ۴.۴ برای بررسی واریانس):
>   - `getStatusCounts` روی یک ماه از ۱M رکورد: **۹–۱۰ms** — قبلاً ۴۱–۴۵ms بود؛ یعنی بیش از ۴ برابر سریع‌تر و اکنون با فاصله‌ی معنادار (نه فقط عبور مرزی) از آستانه‌ی ۵۰ms.
>   - فیلتر ترکیبی (ماه+مشتری+وضعیت) هم به **۱۲ms** بهبود یافت (قبلاً ۱۵–۱۶ms) — بهبود جانبی، چون planner اکنون گزینه‌های ایندکس بیشتری برای انتخاب دارد؛ هدف اصلی این تغییر نبود.
>   - جستجوی پیشوندی نام روی ۱۵,۰۰۰ مشتری بدون تغییر: **۱ms**.
> - **مهم:** این هنوز عدد `node:sqlite` روی CPU کانتینر توسعه است، نه پلاگین `@capacitor-community/sqlite` روی دستگاه واقعی — ریسک ردیف مربوطه در بخش ۶ «حل‌شده در سطح SQL/ایندکس» علامت خورد، اما نه «تأیید‌شده روی دستگاه واقعی» (آن هنوز به‌مانند بقیه‌ی موارد فاز ۱، منتظر آیتم ۱ از «باقی‌مانده‌های فاز ۱» است). این تغییر یک هزینه‌ی جانبی هم دارد که باید صادقانه ثبت شود: یک ایندکس اضافه یعنی هر `INSERT`/`UPDATE` روی جدول `invoices` باید این ایندکس سوم را هم به‌روز نگه دارد — با حجم فعلی تست (seed یک‌میلیونی در ~۴۲–۵۰ ثانیه، مشابه قبل از تغییر) این هزینه قابل‌مشاهده‌ای در seed ایجاد نکرد، ولی این آزمایش مستقیم هزینه‌ی نوشتن تکی (نه bulk-insert) را اندازه نگرفت.
>
> **یادداشت نسخه ۴.۴ (نسخه‌ی قبل): طبق نقشه (بخش «آنچه هنوز باقی مانده تا فاز ۱ کامل شود»، آیتم ۴)، قدم بعدی مشخص و قابل‌اجرا در این محیط (بدون مرورگر/Android) این بود: تست کارایی را از ۲۰۰k رکورد (نسخه‌ی ۴.۲/۴.۳) به **۱ میلیون رکورد** — یعنی خودِ معیار پذیرش رسمی فاز ۱ — ببریم. این کار انجام شد:
> - فایل جدید `tests/perf-test-1m.mjs` (کپی از `perf-test.mjs` با `N_INVOICES=1,000,000` و `N_CUSTOMERS=15,000`) نوشته و روی همان موتور SQLite واقعی (`node:sqlite`) اجرا شد.
> - نتیجه (۳ اجرای مستقل برای بررسی واریانس، نه فقط یک عدد تک‌باره):
>   - فیلتر ترکیبی (ماه+مشتری+وضعیت): **۱۵–۱۶ms** — به‌راحتی زیر آستانه‌ی ۵۰ms و با فاصله‌ی معنادار نسبت به آستانه.
>   - `getStatusCounts` روی یک ماه: **۴۱–۴۵ms** — رد نمی‌شود (زیر ۵۰ms) ولی خیلی به آستانه نزدیک است، برخلاف نتیجه‌ی ۶ms روی ۲۰۰k رکورد در نسخه‌ی ۴.۲/۴.۳.
>   - جستجوی پیشوندی نام روی ۱۵,۰۰۰ مشتری: **۱ms**.
> - این یک یافته‌ی واقعی و جدید است، نه فقط تکرار قبلی: رشد از ۲۰۰k به ۱M رکورد (۵ برابر)، زمان فیلتر ترکیبی را فقط حدود ۴ برابر (۴ms→۱۶ms) ولی زمان `getStatusCounts` را حدود ۷ برابر (۶ms→۴۳ms) افزایش داد — یعنی مقیاس‌پذیری این دو query یکسان نیست. علتِ محتمل: `getStatusCounts` روی این حجم از ایندکس `idx_inv_status_date` عبور می‌کند ولی هنوز باید ~۴ برابر بیشتر ردیف را در طول بازه‌ی یک ماه بشمارد (`COUNT`/`GROUP BY` با حجم واقعی تطبیق داده‌شده)، در حالی که فیلتر ترکیبی با `customerId` هم محدود می‌شود که selectivity خیلی بالاتری دارد. این به جدول ریسک‌ها (بخش ۶) به‌عنوان یک ریسک جدید اضافه شد — نه یک شکست، بلکه یک هشدار برای دستگاه واقعی (که IPC پلاگین native را هم روی این عدد اضافه می‌کند).
> - **مهم:** این هنوز عدد `node:sqlite` روی CPU کانتینر توسعه است، نه پلاگین `@capacitor-community/sqlite` روی یک دستگاه اندروید/iOS واقعی. آیتم ۴ از «باقی‌مانده‌های فاز ۱» را به‌طور کامل بسته نمی‌کند (آن هنوز نیاز به دستگاه واقعی دارد)، ولی شواهد قوی‌تر و در مقیاس واقعی هدف (۱M، نه ۲۰۰k) فراهم کرد — و یک ریسک واقعی (نه نظری) درباره‌ی `getStatusCounts` کشف کرد که در ۲۰۰k رکورد دیده نمی‌شد.
>
> **یادداشت نسخه ۴.۳:** در این نشست، سه اقدام واقعی (نه فقط مستندسازی) انجام شد:
> ۱) کل مجموعه تست (۱۸ assertion در `run-integration-tests.mjs`) دوباره روی SQLite واقعی (node:sqlite) اجرا شد تا از عدم‌رگرسیون نسبت به نسخه‌ی ۴.۲ اطمینان حاصل شود — همه پاس شدند، بدون تغییر.
> ۲) تست کارایی ۲۰۰k رکورد که در نسخه‌ی ۴.۲ فقط به‌صورت یک اسکریپت یک‌بارمصرف اجرا و عددش (۱۴ms) در همین سند ذکر شده بود ولی خودِ اسکریپت هیچ‌جا ذخیره نشده بود (یعنی غیرقابل‌بازتولید بود)، اکنون به‌صورت یک فایل دائمی (`tests/perf-test.mjs`) نوشته و ذخیره شد و دوباره اجرا شد؛ نتیجه‌ی این اجرا: فیلتر ترکیبی ماه+مشتری+وضعیت روی ۲۰۰,۰۰۰ فاکتور = **۲ms**، `getStatusCounts` روی یک ماه = **۶ms**، جستجوی پیشوندی نام روی ۳۰۰۰ مشتری = **۱ms** (جزئیات در بخش ۴، فاز ۱).
> ۳) تلاش شد یک تست واقعی روی مرورگر (با Playwright) برای معیارهای باز فاز ۲.۱/۲.۲/۲.۶ (بدون خطای کنسول، اسکرول لیست ۱۰,۰۰۰+ رکورد با DOM محدود، مهاجرت روی IndexedDB واقعی مرورگر) انجام شود؛ این تلاش **متوقف شد** چون این محیط (sandbox) نه باینری مرورگری (Chromium/Chrome) از قبل نصب دارد و نه دسترسی شبکه برای دانلود آن — این یک محدودیت محیطی مشخص و مستند است (نه یک تصمیم یا کوتاهی)، به جدول ریسک‌ها (بخش ۶) اضافه شد. **مهم:** این یعنی معیارهای پذیرش ۲.۱/۲.۲/۲.۶/آیتم ۳ فاز ۱ («وضعیت فعلی پروژه» بالا) دقیقاً همان چیزی‌اند که در نسخه‌ی ۴.۲ بودند — این نسخه چیزی به آن‌ها اضافه نکرد، فقط علت مشخص و مستند دلیل باز ماندنشان را ثبت کرد.
>
> **یادداشت نسخه ۴.۲:** در این نسخه، پوشه‌ی واقعی پروژه (`html8.html` — همان `4.html` قبلی، به‌همراه `InvoiceRepo.additions.js`) از طریق آپلود مستقیم در دسترس قرار گرفت. با تکیه بر schema و امضای متدهای مستندشده در فاز ۱ این سند، فایل‌های واقعی `src/db/schema.js`، `connection.js`، `InvoiceRepo.js` (با ادغام واقعیِ `InvoiceRepo.additions.js` از طریق `Object.assign`)، `CustomerRepo.js`، و `migrateFromIndexedDB.js` نوشته شدند. چون این محیط به Android SDK/Capacitor دسترسی ندارد، این فایل‌ها با موتور **واقعی SQLite** (ماژول `node:sqlite` در Node 22، نه یک mock دستی) در یک هارنس تست جداگانه (`tests/`) اجرا و صحت‌سنجی شدند — ۱۸ assertion روی منطق SQL واقعی (نه فقط سینتکس) با موفقیت پاس شد، به‌علاوه‌ی یک تست کارایی با ۲۰۰,۰۰۰ فاکتور seed‌شده (فیلتر ترکیبی زیر ۵۰ms هدف فاز ۱). حین این تست‌ها یک باگ واقعی هم کشف و رفع شد (پایین را نگاه کنید). **آنچه هنوز تست نشده:** خودِ پلاگین `@capacitor-community/sqlite` روی یک دستگاه/شبیه‌ساز اندروید واقعی — `node:sqlite` معنایی SQL را تأیید می‌کند، نه اتصال native را. جزئیات کامل در پایان بخش‌های ۱ و ۲.۳/۲.۵ و در جدول ریسک‌ها آمده است.
>
> **باگ کشف‌شده و رفع‌شده در این نسخه:** `migrateFromIndexedDB.js` روی داده‌ی واقعی می‌تواند با فاکتورهایی مواجه شود که به یک `customerId` حذف‌شده اشاره می‌کنند (مشتری در گذشته پاک شده ولی فاکتورهایش مانده — یک حالت واقع‌بینانه در یک اپ که سال‌هاست استفاده می‌شود). چون `invoices.customerId` یک `FOREIGN KEY` به `customers(id)` دارد، insert چنین فاکتوری با خطای «FOREIGN KEY constraint failed» کل batch را rollback می‌کرد. این توسط یک تست ادغام واقعی (نه سینتکسی) کشف شد؛ رفع شد با ساختن یک ردیف جایگزین («مشتری حذف‌شده») برای هر `customerId` یتیم، قبل از insert فاکتورها — نگاه کن به `migrateFromIndexedDB.js` و تست `testOrphanCustomerMigration`.

---

## ۰. وضعیت فعلی پروژه (خلاصه پیشرفت)

| فاز | وضعیت | یادداشت |
|---|---|---|
| فاز ۰ — Baseline | ⏳ انجام‌نشده | هنوز عدد baseline کارایی فعلی روی خودِ اپ (مرورگر/دستگاه واقعی) ثبت نشده؛ فقط یک تست کارایی مستقل روی موتور SQLite (بدون UI) انجام شد — نگاه کن به فاز ۱. تلاش برای اجرای یک تست مرورگری واقعی در نسخه‌ی ۴.۳ به‌خاطر نبود باینری مرورگر و دسترسی شبکه در sandbox متوقف شد (بخش ۶) |
| فاز ۱ — SQLite Native | 🟢 فایل‌های واقعی نوشته و با SQLite واقعی (node:sqlite) تست شدند — هنوز روی Capacitor/دستگاه واقعی تست نشده | schema + هر ۵ فایل Repository/connection/migration به‌صورت کامل نوشته شدند؛ ادغام `InvoiceRepo.additions.js` انجام شد؛ ۱۸ assertion (دوباره در ۴.۳ اجرا و تأیید شد — بدون رگرسیون) + تست کارایی ۲۰۰k رکورد اکنون در `tests/perf-test.mjs` دائمی و بازتولیدپذیر است (۴.۳): فیلتر ترکیبی ۲ms، status counts ۶ms، جستجوی نام ۱ms؛ روی ۱M رکورد (`perf-test-1m.mjs`، ۴.۴/۴.۵): فیلتر ترکیبی ۱۵–۱۶ms، `getStatusCounts` بعد از ایندکس پوششی ۹–۱۰ms؛ هزینه‌ی نوشتن تکی این ایندکس روی همان حجم (`single-write-cost-test.mjs`، ۴.۶): زیر‌میلی‌ثانیه (~۰.۰۱–۰.۰۲ms)؛ حجم واقعی فایل دیتابیس روی دیسک (`db-file-size-test.mjs`، ۴.۷): ~۲۸۰ مگابایت برای ۱M فاکتور + ۱۵k مشتری بعد از VACUUM؛ اثر churn + هزینه/فایده‌ی VACUUM دوره‌ای (`vacuum-churn-test.mjs`، ۴.۸): بدون VACUUM بعد از ۱۰ دور churn (۵٪/دور) حجم ۱.۲۴x پایه می‌شود (+۴۶.۹۱MB بادکردگی خالص)، با VACUUM دوره‌ای حجم حتی از پایه کوچک‌تر می‌ماند ولی هر اجرای VACUUM ~۱.۸ ثانیه روی ۵۰۰k رکورد طول می‌کشد و دیتابیس را حین اجرا قفل می‌کند |
| فاز ۲ — Virtual List + اتصال | 🟡 ریزمرحله ۲.۱ تا ۲.۵ نوشته شد؛ ۴ متد گم‌شده اکنون واقعاً در `InvoiceRepo.js` ادغام و تست شدند (نه فقط فایل مستقل) — هنوز در مرورگر/Capacitor واقعی اجرا نشده (تلاش ۴.۳ برای تست مرورگری متوقف شد، بخش ۶) | به ۶ ریزمرحله تقسیم شد؛ نگاه کن به جدول ریزمرحله‌ها در فاز ۲ |
| فاز ۳ تا ۷ | ⏳ شروع‌نشده | |

> **راهنمای علامت تیک (✅) در این سند:** از این نسخه به بعد، هرجا یک آیتم (فایل، تابع، ریزمرحله) واقعاً نوشته/ساخته شده باشد، کنارش ✅ گذاشته می‌شود — حتی اگر هنوز روی دستگاه/مرورگر واقعی تست نشده باشد (آن مورد جدا و صریح در همان سطر یادداشت می‌شود). یعنی ✅ = «کد/فایل کامل و آماده است»، نه لزوماً «صد‌درصد در دنیای واقعی تأیید شده». مواردی که هنوز شروع نشده یا فقط نیمه‌کاره‌اند، تیک نمی‌خورند (⏳ یا 🟡 می‌مانند).

**تغییر مسیر نسبت به نسخه‌ی ۲.۰:** به‌جای build محلی با Android Studio،
مسیر **GitHub + GitHub Actions** انتخاب شد — یعنی build اندروید (APK) روی
سرورهای GitHub انجام می‌شود، نه روی سیستم توسعه‌دهنده. این تصمیم به بخش
۸ (زیرساخت build) اضافه شده و در فازهای ۰ و ۱ منعکس شده است.

**فایل‌های ساخته‌شده تا این لحظه** (در `rasticpack-capacitor/`):
```
✅ package.json                          وابستگی‌های Capacitor + پلاگین SQLite
✅ capacitor.config.json                 تنظیمات اپ (appId, webDir, ...)
✅ .gitignore                            حذف node_modules/android/ios از ریپو
✅ .github/workflows/build-android.yml   ساخت خودکار APK روی هر پوش
✅ src/db/schema.js                      تعریف جداول + ایندکس‌ها (فاز ۱)
✅ src/db/connection.js                  اتصال idempotent + migration تراکنشی
✅ src/db/InvoiceRepo.js                 getPage / getStatusCounts / insertWithStockUpdate / getTotalsByRange
✅ src/db/CustomerRepo.js                searchByName / getPage / save / remove
✅ src/db/migrateFromIndexedDB.js        مهاجرت یک‌باره با اعتبارسنجی شمارشی
✅ SETUP-PHASE1.md                       راهنمای راه‌اندازی محلی (npm/cap)
✅ BUILD-VIA-GITHUB.md                   راهنمای build از طریق GitHub Actions
```

**فایل‌های جدید (نسخه ۴.۲ — اکنون واقعاً داخل `rasticpack-capacitor/src/db/` نوشته شدند):**
```
✅ src/db/schema.js                      SCHEMA_STATEMENTS + applySchema — شامل ۵ جدول کوچک
                                          (inventory/van_drivers/production_queue/settings_kv/meta)
                                          که در نسخه‌ی ۴.۱ فقط اسم برده شده بودند، نه schema
✅ src/db/connection.js                  getDb idempotent واقعی روی @capacitor-community/sqlite
                                          + withTransaction عمومی — هنوز روی دستگاه واقعی اجرا نشده
✅ src/db/InvoiceRepo.js                 getPage/getStatusCounts/getTotalsByRange/getById/
                                          insertWithStockUpdate/deleteWithStockRestore/bulkInsert
                                          + ادغام واقعی InvoiceRepoAdditions (save/remove/
                                          getMonthCount/getAllDates) با Object.assign
✅ src/db/CustomerRepo.js                searchByName/getPage/findByExactName/save/remove/bulkInsert
✅ src/db/migrateFromIndexedDB.js        بازنویسی کامل با تزریق‌پذیری منبع داده (تست‌پذیر بدون
                                          مرورگر) + رفع باگ FK برای customerId یتیم (نگاه کن به
                                          یادداشت بالای سند)
✅ InvoiceRepo.additions.js              بدون تغییر نسبت به نسخه‌ی آپلودشده — اکنون واقعاً
                                          import و merge شده، نه فقط تست دودی جدا
```

**فایل جدید نسخه ۴.۳ (`tests/perf-test.mjs`):** تست کارایی ۲۰۰k رکورد که در نسخه‌ی
۴.۲ فقط یک اسکریپت یک‌بارمصرف بود (نتیجه‌اش ثبت شد ولی خودِ اسکریپت ذخیره نشده بود)
اکنون یک فایل دائمی در `tests/` است — هرکسی می‌تواند با
`node --experimental-sqlite perf-test.mjs` دوباره اجرا و عدد را تأیید کند.
همچنین در همین نشست تأیید شد که `tests/_node_test_copies/*.js` هیچ واگرایی
منطقی با فایل‌های واقعی `rasticpack-capacitor/src/db/*.js` ندارند — تنها تفاوت
در هر سه فایل، خط `import { getDb } from './connection.js'` است که در کپی تست
به `../testConnection.node-only.mjs` تغییر کرده (همان‌طور که در `tests/README.md`
مستند شده) — یعنی تست‌ها واقعاً همان منطقی را می‌آزمایند که در فایل‌های واقعی است،
نه یک نسخه‌ی قدیمی‌شده یا دستکاری‌شده.

**تست شد، نه فقط نوشته شد:** یک هارنس تست جدا (`tests/`، خارج از پروژه‌ی
اصلی) با `node:sqlite` (موتور واقعی SQLite در Node 22، نه mock) این ۵ فایل
را به‌صورت end-to-end اجرا کرد: ۱۸ assertion (CRUD مشتری/فاکتور، فیلتر
ترکیبی ماه+مشتری+وضعیت، تراکنش atomic موجودی، مهاجرت با شمارش تطبیقی،
جلوگیری از اجرای دوباره‌ی مهاجرت) همگی پاس شدند. یک تست کارایی مستقل با
۲۰۰,۰۰۰ فاکتور seed‌شده هم اجرا شد: فیلتر ترکیبی (ماه+مشتری+وضعیت) در ۱۴ms
جواب داد — جهت‌گیری همسو با معیار پذیرش فاز ۱ (< ۵۰ms)، هرچند این عدد روی
۲۰۰k رکورد و موتور Node است، نه ۱M رکورد روی دستگاه موبایل واقعی (آن تست
واقعی هنوز به فاز ۷ موکول است).

**آنچه هنوز باقی مانده تا فاز ۱ کامل شود:**
1. اجرای واقعی این فایل‌ها داخل یک پروژه‌ی Capacitor واقعی روی اندروید/iOS
   (پلاگین `@capacitor-community/sqlite` هنوز هرگز روی یک دستگاه/شبیه‌ساز
   واقعی صدا زده نشده — این محیط بدون Android SDK و بدون دسترسی شبکه است)
2. اتصال واقعی `InvoiceRepo`/`CustomerRepo` به کد رندر داخل `html8.html`
   (جایگزینی آرایه‌های `invoices`/`customers` در حافظه با صداکردن Repository)
   — کد سمت `html8.html` از قبل این اتصال را از طریق `window.__InvoiceRepo`
   پیش‌بینی کرده (نگاه کن به فاز ۲)؛ آنچه مانده صرفاً اجرای واقعی import
   ماژول‌ها در یک build واقعی Capacitor است، نه نوشتن کد بیشتر
3. اجرای واقعی `migrateFromIndexedDB.js` روی یک نسخه‌ی تست از IndexedDB واقعی
   مرورگر (نه منبع داده‌ی fake تزریق‌شده در تست‌های این نسخه)
4. اندازه‌گیری واقعی معیار پذیرش با ۱ میلیون رکورد (< ۵۰ms روی فیلتر ترکیبی)
   روی خودِ دستگاه — بخش «۱M رکورد روی موتور Node» در نسخه‌ی ۴.۴ انجام شد
   (`tests/perf-test-1m.mjs`: فیلتر ترکیبی ۱۵–۱۶ms) و ریسک `getStatusCounts`
   کشف‌شده در همان نسخه با یک ایندکس پوششی در نسخه‌ی ۴.۵ برطرف شد (اکنون
   ۹–۱۰ms، نه ۴۱–۴۵ms)؛ آنچه هنوز باز است فقط «روی دستگاه واقعی» است، نه
   «روی ۱M رکورد»
5. تأیید build موفق روی GitHub Actions (اجرای واقعی workflow)

به دلیل اینکه فاز ۱ و ۲ باید با هم دیده شوند (طبق بخش ۷)، مرحله‌ی (۲) در
بالا عملاً باید همزمان با شروع فاز ۲ انجام شود، نه قبل از آن.

---

## ۱. خلاصه مدیریتی (Executive Summary)

| مورد | وضعیت فعلی | وضعیت هدف |
|---|---|---|
| محل ذخیره | IndexedDB (key-value ساده) + آرایه‌ی JS در RAM | SQLite native با ایندکس چندستونی |
| نحوه فیلتر/جستجو | لود کامل آرایه → `.filter()` در JS | Query مستقیم SQL با `WHERE` + `INDEX` |
| رندر لیست | Pagination دستی (۵۰ آیتم + دکمه «بیشتر») | Virtual List (فقط ۲۰-۳۰ DOM node فعال) |
| محاسبات آماری | حلقه‌ی JS روی کل `invoices` | `SUM/GROUP BY` در SQL یا Web Worker |
| سقف عملی فعلی | ~۵۰,۰۰۰ - ۱۰۰,۰۰۰ رکورد (شروع لگ) | نامحدود (وابسته به فضای دیسک، نه RAM) |
| پلتفرم اجرا | WebView / مرورگر | Capacitor + پلاگین SQLite native |

**نکته کلیدی معماری:** گلوگاه فعلی «حافظه» است (همه‌چیز در RAM لود می‌شود)؛ گلوگاه هدف باید «دیسک با ایندکس» باشد — دقیقاً مدلی که هر دیتابیس تولیدی واقعی استفاده می‌کند.

---

## ۲. اصول طراحی (قبل از نوشتن هر خط کد)

1. **هیچ‌گاه تمام یک جدول را در حافظه لود نکن.** هر query باید `LIMIT` داشته باشد.
2. **فیلتر/سورت/جمع باید در لایه‌ی دیتابیس انجام شود، نه در JavaScript.** موتور SQLite برای این کار بهینه شده؛ حلقه‌ی JS نه.
3. **رندر باید متناسب با viewport باشد، نه متناسب با تعداد داده.** ۱۰۰ یا ۱۰ میلیون رکورد باید یک تعداد ثابت DOM node بسازد.
4. **هر نوشتن چندمرحله‌ای باید تراکنشی (atomic) باشد.** یک کرش وسط کار نباید داده را نصفه‌کاره بگذارد.
5. **لایه‌ی داده و لایه‌ی UI باید جدا باشند (Repository Pattern).** تغییر موتور ذخیره‌سازی در آینده نباید به رندر/HTML دست بزند.

---

## ۳. معماری هدف (نمای کلی)

```
┌─────────────────────────────────────────────┐
│                   UI Layer                   │
│   VirtualList · CardRenderer · SearchBox      │
└───────────────────┬───────────────────────────┘
                     │  (فقط درخواست صفحه/فیلتر، بدون منطق SQL)
┌───────────────────▼───────────────────────────┐
│              Repository Layer                 │
│  InvoiceRepo · CustomerRepo · StatsRepo        │
│  (تنها نقطه‌ای که با دیتابیس صحبت می‌کند)        │
└───────────────────┬───────────────────────────┘
                     │
┌───────────────────▼───────────────────────────┐
│         SQLite (native, via Capacitor)         │
│   جداول ایندکس‌شده + FTS5 + تراکنش‌ها            │
└─────────────────────────────────────────────────┘
```

**قانون طلایی:** کد رندر (مثل `renderInvoicesList` فعلی) هرگز مستقیم به دیتابیس دسترسی ندارد — فقط از `InvoiceRepo.getPage(...)` صدا می‌زند. این جداسازی یعنی اگر فردا خواستی موتور را عوض کنی (مثلاً به یک بک‌اند remote)، فقط لایه‌ی Repository تغییر می‌کند.

---

## ۴. فازهای اجرا

### فاز ۰ — آماده‌سازی و خط‌مبنا (Baseline)
**هدف:** قبل از تغییر، بدانیم دقیقاً چه چیزی را بهبود می‌دهیم.

- بکاپ کامل کد فعلی + یک نسخه‌ی جدا برای توسعه (branch)
- مشخص کردن حجم هدف واقعی (۱ میلیون؟ ۱۰ میلیون؟ نامحدود نظری؟) — این عدد، تصمیمات ایندکس‌گذاری فاز ۱ را تعیین می‌کند
- اندازه‌گیری کارایی فعلی با ابزار Performance مرورگر روی یک نمونه (مثلاً ۱۰,۰۰۰ رکورد فیک) تا معیار «قبل» ثبت شود
- Capacitor راه‌اندازی شود (در صورت نبودن) چون فاز ۱ نیاز به پلاگین native دارد

**خروجی قابل تحویل:** سند baseline (اعداد کارایی فعلی) + پروژه‌ی Capacitor آماده

---

### فاز ۱ — موتور ذخیره‌سازی: IndexedDB → SQLite Native
**چرا:** IndexedDB فقط key-value است؛ query ترکیبی (ماه + وضعیت + مشتری با هم) در آن نیاز به فیلتر دستی در JS دارد. SQLite با یک عبارت SQL و ایندکس، همین کار را در چند میلی‌ثانیه انجام می‌دهد.

**وضعیت:** 🟡 اسکلت کامل ساخته شده (schema + connection + دو Repository + اسکریپت مهاجرت) — هنوز به `4.html` وصل نشده و هنوز تست سرعت واقعی (معیار پذیرش) انجام نشده.

**اقدامات انجام‌شده:**

✅ ۱. ساختار پروژه‌ی Capacitor راه‌اندازی شد (`package.json`، `capacitor.config.json`) با وابستگی:
```json
"@capacitor-community/sqlite": "^6.0.0"
```

✅ ۲. schema نهایی در `src/db/schema.js` (نسخه‌بندی‌شده با `SCHEMA_VERSION` برای migration‌های آینده):
```sql
CREATE TABLE customers (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL COLLATE NOCASE,
  company TEXT, address TEXT, phone TEXT,
  lat REAL, lng REAL, locationLink TEXT,
  createdAt TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_cust_name  ON customers(name);
CREATE INDEX idx_cust_phone ON customers(phone);

CREATE TABLE invoices (
  id INTEGER PRIMARY KEY,
  customerId INTEGER NOT NULL,
  customerName TEXT NOT NULL,
  date TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'paid',
  sent INTEGER NOT NULL DEFAULT 0,
  sentToProduction INTEGER NOT NULL DEFAULT 0,
  paidAmount REAL,
  totalSheets INTEGER NOT NULL DEFAULT 0,
  editedAt TEXT,
  data TEXT NOT NULL,              -- items[] و بقیه‌ی فیلدهای متغیر، به‌صورت JSON
  FOREIGN KEY(customerId) REFERENCES customers(id)
);
CREATE INDEX idx_inv_cust        ON invoices(customerId);
CREATE INDEX idx_inv_date        ON invoices(date);
CREATE INDEX idx_inv_status      ON invoices(status);
CREATE INDEX idx_inv_sent        ON invoices(sent);
CREATE INDEX idx_inv_cust_date   ON invoices(customerId, date);   -- فیلتر مشتری+بازه‌ی تاریخ با هم
CREATE INDEX idx_inv_status_date ON invoices(status, date);       -- فیلتر وضعیت+تاریخ (تب آمار/بدهکاران)

-- جداول کوچک بدون رشد نامحدود: inventory، van_drivers، production_queue، settings_kv، meta
```
schema آماده‌ی FTS5 برای فاز ۳ نیز از پیش نوشته شده (غیرفعال تا فاز ۳).

✅ ۳. لایه‌ی Repository با امضای دقیقاً هم‌راستا با پیشنهاد این سند (`src/db/InvoiceRepo.js`، `src/db/CustomerRepo.js`):
```js
InvoiceRepo.getPage({ month, exactDate, status, customerId, search, limit, offset })
  // → { items, total } — pagination واقعی با LIMIT/OFFSET در SQL، نه slice روی آرایه

InvoiceRepo.getStatusCounts({ month, exactDate, customerId, search })
  // → { draft, partial, paid, debtor } — یک GROUP BY به‌جای حلقه‌ی JS
  // (معادل renderInvStatusCounts فعلی در 4.html)

InvoiceRepo.getTotalsByRange(start, end)
  // → { count, turnover, profit } — SUM از طریق json_extract روی ستون data

InvoiceRepo.insertWithStockUpdate(invoice, stockDeductions)
  // تراکنشی: کسر موجودی + درج فاکتور در یک BEGIN/COMMIT (پیش‌نمایش فاز ۵)

InvoiceRepo.deleteWithStockRestore(id, stockRestorations)
  // تراکنشی: بازگرداندن موجودی + حذف فاکتور

InvoiceRepo.bulkInsert(invoices)
  // batch insert داخل یک تراکنش — برای مهاجرت اولیه و Restore بکاپ (فاز ۵/۶)

CustomerRepo.searchByName(query, { limit, offset, prefix })
CustomerRepo.getPage({ limit, offset })
CustomerRepo.findByExactName(name)   // معادل findCustomerByName فعلی
CustomerRepo.insert / save / remove / bulkInsert
```

✅ ۴. لایه‌ی اتصال (`src/db/connection.js`): اتصال idempotent (باز کردن مکرر مشکلی ایجاد نمی‌کند)، migration اولیه‌ی schema داخل یک تراکنش، و `withTransaction(fn)` عمومی برای فاز ۵.

✅ ۵. اسکریپت مهاجرت یک‌باره (`src/db/migrateFromIndexedDB.js`):
   - می‌خواند از IndexedDB فعلی (`rasticpackDB`) و، در صورت خالی بودن، از fallback قدیمی‌تر در `window.storage['rasticpack-data']`
   - می‌نویسد در SQLite با `bulkInsert` دسته‌ای (۵۰۰تایی) تا فشار حافظه کم بماند
   - **قبل از حذف هیچ داده‌ای**، یک شمارش تطبیقی انجام می‌دهد (`COUNT(*)` مقصد ≥ تعداد مبدأ)؛ اگر ناقص بود، خطا می‌دهد و داده‌ی IndexedDB دست‌نخورده می‌ماند (پیاده‌سازی مستقیم راهکار ریسک شماره‌ی ۱ در بخش ۶)
   - با پرچم `settings_kv['migrated_v1']` از اجرای دوباره جلوگیری می‌کند

**اقدامات باقی‌مانده تا این فاز کامل شود:**

- [ ] وصل کردن `InvoiceRepo`/`CustomerRepo` به توابع رندر واقعی در `4.html`
      (این کار عمداً به فاز ۲ موکول شده — نگاه کن به یادداشت پایان بخش ۷)
- [ ] اجرای `migrateFromIndexedDB.js` روی یک نسخه‌ی تست واقعی از داده‌ی کاربر
- [ ] نوشتن اسکریپت seed مقدماتی (نسخه‌ی کوچک فاز ۷) برای تولید ۱ میلیون ردیف تستی و اندازه‌گیری واقعی معیار پذیرش
- [ ] تأیید build سالم روی GitHub Actions (نگاه کن به بخش ۸)

**معیار پذیرش فاز:** یک `SELECT` با فیلتر ماه+مشتری+وضعیت روی ۱ میلیون ردیف تست، زیر ۵۰ میلی‌ثانیه جواب بدهد. *(هنوز روی دستگاه واقعی اندازه‌گیری نشده — آن بخش ردیف باز می‌ماند. اما اندازه‌گیری روی خودِ ۱M ردیف [نه فقط ۲۰۰k] اکنون در نسخه‌ی ۴.۴ با `tests/perf-test-1m.mjs` روی موتور node:sqlite انجام شد: فیلتر ترکیبی در ۱۵–۱۶ms جواب داد [PASS با فاصله‌ی خوب از آستانه]، اما `getStatusCounts` روی همان حجم ۴۱–۴۵ms شد [PASS ولی خیلی نزدیک به آستانه‌ی ۵۰ms — نگاه کن به ریسک جدید در بخش ۶]. این هنوز جایگزین تست واقعی روی موبایل نیست، ولی شواهد در مقیاس هدف واقعی [۱M، نه ۲۰۰k] است، نه فقط جهت‌گیری کلی.)*

---

### فاز ۲ — رندر: Virtual List واقعی + اتصال Repository به `4.html`
**چرا:** حتی با query سریع، ساخت صدها المان HTML همزمان، thread اصلی مرورگر را قفل می‌کند. علاوه بر این، طبق بررسی فاز ۱، حدود ۳۰ تابع داخل `4.html` (نه فقط `renderInvoicesList`) مستقیم با آرایه‌ی `invoices` در حافظه کار می‌کنند — پس این فاز باید هم Virtual List بسازد و هم این وابستگی‌ها را جمع کند.

**تصمیم معماری (به‌جای بازنویسی یک‌جای کل فایل):**
به‌خاطر ریسک بالای بازنویسی همزمان ۳۰+ تابع در یک فایل ۵۹۰۰ خطی، این فاز
به ریزمرحله‌های مستقل و قابل‌تحویل جدا تقسیم شد. هر ریزمرحله به‌تنهایی
قابل تست است و فایل را در حالت کارکردن نگه می‌دارد (نه یک commit غول‌پیکر
که وسط راه اپ را می‌شکند).

**ریزمرحله‌های فاز ۲:**

| # | ریزمرحله | توضیح | وضعیت |
|---|---|---|---|
| ✅ ۲.۱ | اتصال پایه (bootstrap) | افزودن `<script type="module">` به `4.html`، import کردن `InvoiceRepo`/`CustomerRepo`/`migrateFromIndexedDB`، اجرای مهاجرت یک‌بار در startup، بدون تغییر هیچ تابع رندر موجود (سیستم قدیم و SQLite هر دو زنده می‌مانند) | ✅ کد در `4.html` موجود است؛ در این پاسخ فقط یک هوک اضافه شد (`window.__onSQLiteReady`) تا اگر SQLite کمی بعد از اولین رندر آماده شود، تب فاکتورها (در صورت باز بودن) یک‌بار دیگر خودش را رفرش کند |
| ✅ ۲.۲ | جایگزینی `renderInvoicesList` با Virtual List | تنها این یک تابع از آرایه‌ی حافظه جدا و به `InvoiceRepo.getPage()` async وصل می‌شود؛ کامپوننت `VirtualList` نوشته می‌شود؛ فیلترهای ماه/وضعیت/مشتری/جستجو به پارامترهای query تبدیل می‌شوند | ✅ کد کامل نوشته شد (`createVirtualList`، `fetchInvoicesPage`، بازنویسی `renderInvoicesList`) و از نظر سینتکس با `node --check` تأیید شد. ⚠️ **تست میدانی باقی مانده:** روی مرورگر/دستگاه واقعی با ۱۰,۰۰۰+ رکورد و اتصال زنده به `InvoiceRepo.getPage()` هنوز اجرا نشده (پروژه‌ی `rasticpack-capacitor/` در این محیط در دسترس نبود) |
| ۲.۳ | وصل‌کردن توابع تک‌فاکتوری (~۲۰ تابع با الگوی `invoices.find(i=>i.id===invId)`) | `markInvoiceSent`, `markInvoiceSettled`, `deleteInvoice`, `saveInvoiceEdit`, `sendAllToProduction` و مشابه — همگی به `InvoiceRepo.getById()`/`save()`/`deleteWithStockRestore()` async وصل می‌شوند؛ چون کم‌ریسک‌ترین و مستقل‌ترین توابع‌اند، به‌صورت دسته‌ای انجام می‌شوند | ✅ **با کمتر از حد تصور تغییر انجام شد** — همه‌ی ~۱۳ نقطه‌ی نوشتن/حذف فاکتور در کل اپ از قبل از دو تابع گلوگاه `dbSaveInvoice`/`dbDeleteInvoiceRecord` عبور می‌کردند (نه اینکه مستقیم با IndexedDB کار کنند)؛ پس به‌جای بازنویسی تک‌تک ۲۰ تابع، همین دو گلوگاه به `InvoiceRepo` وصل شدند و همه‌ی توابع بالا خودکار پوشش داده شدند. جزئیات و دلیل انتخاب `save`/`remove` به‌جای `insertWithStockUpdate`/`deleteWithStockRestore` در متن پایین همین بخش |
| ✅ ۲.۴ | وصل‌کردن `renderCustInvoicesModal` و `renderInvStatusCounts` | این دو از `InvoiceRepo.getPage()`/`getStatusCounts()` تغذیه می‌شوند | ✅ کد کامل نوشته شد و با `node --check` (هر سه بلاک `<script>` فایل، شامل بلاک `type="module"`) تأیید شد. **یک تغییر آگاهانه نسبت به طرح اولیه:** برای مودال فاکتورهای مشتری، به‌جای `createVirtualList` (ریزمرحله ۲.۲)، از همان الگوی «نمایش بیشتر»ی که `renderCustomersList` از قبل دارد استفاده شد — چون `createVirtualList` بر مبنای اسکرول *سراسری صفحه* (`window.scrollY`) کار می‌کند، اما این مودال یک container مستقل با `overflow-y:auto` دارد؛ فرمول ویوپورت فعلی VirtualList برای یک container داخلی نادرست جواب می‌دهد. جزئیات کامل و دلیل انتخاب در پایین همین بخش. ⚠️ **تست میدانی باقی مانده:** مثل ریزمرحله‌های قبل، فقط سینتکس/منطق بررسی شده (شبیه‌سازی عددی fallback شمارش وضعیت‌ها با ۲۰۰۰ سناریوی تصادفی، بدون هیچ عدم‌تطابقی با منطق قبلی) — نه اجرای واقعی روی مرورگر/Capacitor |
| ✅ ۲.۵ | وصل‌کردن `renderInvMonthSwitcher`/`buildInvMonthList` (سوییچر ماه شمسی) | این تابع فعلاً با شمارش دستی روی کل آرایه، لیست ماه‌های دارای فاکتور را می‌سازد؛ باید با یک `SELECT DISTINCT strftime(...)` یا معادل جایگزین شود | ✅ کد کامل نوشته شد و با `node --check` (هر سه بلاک `<script>` فایل) تأیید شد. **یک ریسک قدیمی‌تر (از ۲.۲/۲.۴) همین‌جا حل شد**: تابع جدید `monthSortValToIsoRange` مقدار sortVal شمسی را قبل از رسیدن به Repo به `{monthStart, monthEnd}` میلادی/ISO تبدیل می‌کند — و `fetchInvoicesPage`/`fetchInvoiceStatusCounts` (۲.۲/۲.۴) هم برای هم‌راستایی به همین قرارداد به‌روزرسانی شدند. جزئیات کامل در پایین همین بخش. ⚠️ **تست میدانی باقی مانده:** مثل ریزمرحله‌های قبل، فقط سینتکس/منطق بررسی شده، نه اجرای واقعی روی مرورگر/Capacitor |
| ۲.۶ | حذف کامل آرایه‌ی `invoices` از حافظه + حذف کد IndexedDB قدیمی | فقط بعد از تأیید کامل ۲.۱ تا ۲.۵ روی داده‌ی واقعی انجام می‌شود — تا آن زمان هر دو سیستم (آرایه‌ی قدیمی + SQLite) به‌صورت موازی نگه داشته می‌شوند تا در صورت باگ، rollback فوری ممکن باشد | ⏳ بعدی، مشروط |

**نکته‌ی مهم درباره‌ی توابع دیگر (`computeStatsData`, `computeStatsLeaders`):**
این‌ها به فاز ۴ (آمار) تعلق دارند نه فاز ۲ — طبق نقشه‌ی اصلی (بخش ۷)، فاز ۳ و ۴ می‌توانند بعد از فاز ۲ به‌صورت موازی اجرا شوند. پس در فاز ۲ دست‌نخورده می‌مانند و همچنان از آرایه‌ی حافظه می‌خوانند تا فاز ۴ برسد.

**اقدامات فنی هر ریزمرحله (خلاصه‌ی روش):**

✅ ۱. کامپوننت عمومی `VirtualList` — **ساخته شد**، با یک تغییر آگاهانه نسبت به طرح اولیه‌ی این سند:
   - ارتفاع کل لیست از `total` (طول آرایه‌ی فیلترشده، یا در آینده مقدار `total` برگشتی `InvoiceRepo.getPage()`) به‌صورت **تخمینی** (`estimateItemHeight` ثابت) محاسبه و به‌عنوان دو spacer بالا/پایین نمایش داده می‌شود
   - فقط ردیف‌های داخل viewport + بافر بالا/پایین (پیش‌فرض ۶ آیتم) واقعاً در DOM ساخته می‌شوند
   - **تغییر نسبت به طرح اولیه:** به‌جای `IntersectionObserver` و «recycle کردن node های موجود»، پیاده‌سازی نهایی از `window.addEventListener('scroll'/'resize', …, {passive:true})` + `requestAnimationFrame` استفاده می‌کند و در هر رندر، `itemsHost.innerHTML` بازه‌ی جدید را کامل جایگزین می‌کند (نه recycle واقعی گره‌به‌گره). دلیل تغییر: صفحه‌ی فاکتورها با یک اسکرول سراسری صفحه (`body`) کار می‌کند نه یک container داخلی با `overflow:auto` — و کارت‌های فاکتور با باز/بسته شدن آکاردئون ارتفاع متغیر دارند، که با `IntersectionObserver` روی سنتینل‌های ثابت‌فاصله ناسازگار است. رویکرد spacer-div ساده‌تر، برای ارتفاع متغیر مقاوم‌تر، و برای پیاده‌سازی/تست دستی در یک فایل تکی HTML عملی‌تر بود. عیب شناخته‌شده: چون ارتفاع تخمینی است (نه اندازه‌گیری‌شده)، اسکرول‌بار در لیست‌های خیلی بزرگ ممکن است کمی نادقیق باشد — مشکل صحت داده ایجاد نمی‌کند.
   - ⬜ تست RTL/فارسی روی داده‌ی واقعی هنوز باقی مانده (نگاه کن به بخش ۶، ریسک RTL) — این پیاده‌سازی تاکنون فقط از نظر سینتکس با `node --check` بررسی شده، نه در یک مرورگر واقعی

✅ ۲. اتصال به Repository: `renderInvoicesList` اکنون به‌ازای هر بازه‌ی دیده‌شده، `fetchInvoicesPage(offset,limit,ctx)` را صدا می‌زند؛ این تابع اگر `window.__InvoiceRepo.getPage` در دسترس باشد از آن استفاده می‌کند (و در صورت خطا، بی‌صدا به آرایه‌ی حافظه برمی‌گردد)، وگرنه مستقیماً از آرایه‌ی از قبل فیلترشده `slice` می‌گیرد — دقیقاً همان الگوی «هر دو سیستم هم‌زمان زنده» که در بخش ۶ پیش‌بینی شده بود

✅ ۳. توابع تک‌فاکتوری (ریزمرحله ۲.۳) — **کشف معماری که کار را ساده‌تر کرد:** قبل از شروع، فرض این بود که باید ~۲۰ تابع را یکی‌یکی به `InvoiceRepo` وصل کنیم. بررسی کد نشان داد همه‌ی این توابع (`markInvoiceSent`, `unmarkInvoiceSent`, `markInvoiceSettled`, `unmarkInvoiceSettled`, `submitInvoicePayment`, `sendAllToProduction`, `saveInvoiceEdit`, `submitCalc2Invoice`, `deleteInvoice`, و همگام‌سازی نام مشتری در `saveCustomerEdit`) از قبل، برای نوشتن در IndexedDB، از دو تابع کمکی مشترک عبور می‌کردند:
```js
function dbSaveInvoice(inv){ ... }
function dbDeleteInvoiceRecord(id){ ... }
```
پس به‌جای دست‌کاری ۲۰ نقطه، همین دو گلوگاه به `InvoiceRepo` وصل شدند:
```js
function dbSaveInvoice(inv){
  if(!inv) return;
  idbPut('invoices',inv);                              // مسیر قدیمی — همیشه اجرا می‌شود (تا فاز ۲.۶)
  if(window.__InvoiceRepo?.save)
    window.__InvoiceRepo.save(inv).catch(e=>console.debug('...',e?.message));
}
function dbDeleteInvoiceRecord(id){
  idbDelete('invoices',id);
  if(window.__InvoiceRepo?.remove)
    window.__InvoiceRepo.remove(id).catch(e=>console.debug('...',e?.message));
}
```
**نکته‌ی طراحی API (تغییر نسبت به فاز ۱):** فاز ۱ فقط `insertWithStockUpdate`/`deleteWithStockRestore` (تراکنشی، با عوارض جانبی روی موجودی) را برای `InvoiceRepo` مستند کرده بود. اما در این ریزمرحله، منطق کسر/بازگردانی موجودی هنوز در جاوااسکریپت انجام می‌شود (روی آرایه‌ی `inventory`) — پس استفاده از آن دو متد اینجا یا اشتباه بود (عارضه‌ی موجودی را دوباره اعمال می‌کرد) یا نیاز به بازنویسی هم‌زمان منطق موجودی داشت (که خارج از دامنه‌ی ۲.۳ است). بنابراین دو متد عمومیِ **بدون عارضه‌ی جانبی** لازم بود:
```js
InvoiceRepo.save(invoice)   // upsert ساده‌ی یک ردیف — بدون تغییر موجودی
InvoiceRepo.remove(id)      // حذف ساده‌ی یک ردیف — بدون تغییر موجودی
```
⚠️→✅ **این دو متد در فاز ۱ مستند نشده بودند؛ در نسخه‌ی ۴.۲ ادغام واقعی انجام شد.** این دو متد (به‌همراه دو متد دیگر مربوط به ریزمرحله‌ی ۲.۵) در `InvoiceRepo.additions.js` نوشته شده بودند؛ در این نسخه، `src/db/InvoiceRepo.js` واقعی نوشته شد و دقیقاً با همان الگوی پیشنهادی خودِ فایل ادغام شدند: `Object.assign(InvoiceRepo, InvoiceRepoAdditions(getDb))`. امضای `connection.js` واقعی (که هم در این نسخه نوشته شد) دقیقاً با فرض `db.query`/`db.run` این فایل هم‌راستا ساخته شد، پس نیازی به تطبیق نبود. صحت این ادغام با یک تست ادغام واقعی روی SQLite (node:sqlite) تأیید شد (نه فقط سینتکس). متدهای تراکنشی/موجودی‌محور (`insertWithStockUpdate`/`deleteWithStockRestore`) هم در همین نسخه نوشته و با یک سناریوی واقعی (کسر/بازگردانی موجودی درون تراکنش) تست شدند؛ برای صدا زده‌شدن واقعی از UI همچنان به فاز ۵ موکول‌اند.

⬜ **خارج از دامنه‌ی ۲.۳ (عمداً):** مسیرهای bulk — `doRestore` (بازیابی بکاپ) و `clearAllData` — همچنان فقط IndexedDB را می‌نویسند/پاک می‌کنند، نه SQLite. این دقیقاً همان کاری‌ست که فاز ۵ به‌صراحت وعده داده («Batch insert برای Restore بکاپ») — پس عمداً اینجا دست نخورد تا با batch/تراکنش درست، یک‌جا در فاز ۵ انجام شود؛ تا آن زمان، اگر کسی بکاپ را روی نسخه‌ای با SQLite فعال بازیابی کند، SQLite تا اجرای بعدی مهاجرت به‌روز نمی‌شود — این را به‌عنوان یک ریسک شناخته‌شده به بخش ۶ اضافه کردم.

✅ ۴. مودال فاکتورهای مشتری و شمارش وضعیت‌ها (ریزمرحله ۲.۴) — **دو تصمیم معماری مهم گرفته شد:**

**تصمیم اول — چرا مودال از `createVirtualList` استفاده نکرد:**
طرح اولیه گفته بود مودال هم «از همان الگوی Virtual List/query استفاده می‌کند»، اما در عمل `createVirtualList` (ریزمرحله ۲.۲) صریحاً بر مبنای اسکرول *سراسری صفحه* طراحی شده (`window.scrollY`، نگاه کن به `computeRange` در همان تابع) — چون تب فاکتورها با اسکرول کل صفحه کار می‌کند، نه یک container داخلی. مودال فاکتورهای مشتری برعکس است: یک باکس ثابت (`position:fixed`) با `overflow-y:auto` مستقل روی `cust-inv-modal-list`، در حالی که خودِ صفحه‌ی پشتش اسکرول نمی‌شود. اگر `createVirtualList` بدون تغییر روی این container استفاده می‌شد، فرمول ویوپورت غلط جواب می‌داد (چون `window.scrollY` ربطی به اسکرول داخلی مودال ندارد). به‌جای تعمیم فوری VirtualList به «آگاه از container داخلی» (که ریسک و دامنه‌ی بیشتری می‌طلبید)، از همان الگوی ساده‌ی «نمایش بیشتر» که `renderCustomersList` از قبل با موفقیت استفاده می‌کند، استفاده شد: `custInvModalRenderLimit` (شروع از `PAGE_SIZE=۵۰`) + دکمه‌ی پایین لیست که با هر کلیک آن را افزایش می‌دهد. برای یک مودال که فاکتورهای *یک* مشتری (نه کل دیتابیس) را نشان می‌دهد، این سطح از windowing کافی است؛ signature تابع (`renderCustInvoicesModal()` بدون آرگومان) طوری نگه داشته شد که در آینده بدون تغییر بیرونی قابل جایگزینی با یک VirtualList نسخه‌ی «container-aware» باشد.

پیاده‌سازی:
```js
async function fetchCustomerInvoicesPage(offset,limit,customerId,fullList){
  if(window.__InvoiceRepo?.getPage){
    try{
      const res=await window.__InvoiceRepo.getPage({customerId,limit,offset});
      if(res && Array.isArray(res.items)) return res.items.map(i=>({type:'final',...i}));
    }catch(e){ /* بی‌صدا به fullList برمی‌گردیم */ }
  }
  return fullList.slice(offset,offset+limit);
}
```
یک شماره‌ی نسخه (`_custInvModalSeq`، دقیقاً هم‌الگو با `seq` در `createVirtualList`) اضافه شد تا اگر کاربر سریع مودال را ببندد یا مشتری دیگری باز کند، پاسخ `await` دیرآمده‌ی fetch قبلی نادیده گرفته شود و روی لیست اشتباه رندر نکند.

**تصمیم دوم — قرارداد `InvoiceRepo.getStatusCounts` و مرز دامنه‌ی خلاصه‌ی بدهکاران:**
طبق مستندات فاز ۱، امضای این متد `{ month, exactDate, customerId, search }` است — **بدون** پارامتر `status`، چون این تابع باید هر ۴ شمارش (`draft/partial/paid/debtor`) را همزمان برگرداند تا روی هر ۴ دکمه‌ی فیلتر نمایش داده شود، صرف‌نظر از این‌که در همین لحظه کدام دکمه فعال است. منطق fallback (وقتی repo در دسترس نیست) دقیقاً همان منطق قبلی `renderInvStatusCounts` است — این با یک شبیه‌سازی ۲۰۰۰ سناریوی تصادفی (بدون DOM) در Node تأیید شد که صفر عدم‌تطابق داشت.

اما **خلاصه‌ی بدهکاران** (`renderDebtorSummary` — کارت بنفش بالای لیست با مبلغ کل مانده + تعداد مشتری بدهکار) عمداً در دامنه‌ی این ریزمرحله قرار نگرفت: این محاسبه فقط یک عدد شمارشی نیست، بلکه نیاز به فهرست واقعی فاکتورهای بدهکار دارد تا مبلغ مانده‌ی هر کدام (`invoiceRemaining`) را جمع بزند و بر اساس مشتری گروه‌بندی کند — یعنی یک aggregation پیچیده‌تر از `COUNT`/`GROUP BY` ساده که `getStatusCounts` برمی‌گرداند. این هنوز فقط از `baseList` (آرایه‌ی حافظه) محاسبه می‌شود؛ اتصال آن به یک تابع تجمیعی SQL واقعی (`SUM` روی `json_extract(data,'$.paidAmount')` یا مشابه) به فاز ۴ (محاسبات آماری) موکول شد — همان فازی که طبق نقشه‌راه اصلی مسئول انتقال Aggregation از JS به SQL است.

⬜ **خارج از دامنه‌ی ۲.۴ (عمداً):** `renderInvMonthSwitcher`/`buildInvMonthList` (سوییچر ماه شمسی بالای تب فاکتورها) دست‌نخورده ماند — این دقیقاً ریزمرحله‌ی ۲.۵ است، نه ۲.۴؛ همچنان با شمارش دستی روی کل آرایه‌ی `invoices` کار می‌کند.

✅ ۵. سوییچر ماه شمسی و رفع ریسک «پارامتر ماه» (ریزمرحله ۲.۵) — **یک تصمیم معماری مهم گرفته شد که ریسک قدیمی‌تر از ۲.۲/۲.۴ را هم حل می‌کند:**

**رفع ریسک «پارامتر ماه»:** طبق آنچه در پایان ریزمرحله‌ی ۲.۴ مستند شده بود، `ctx.month` که به `InvoiceRepo.getPage`/`getStatusCounts` فرستاده می‌شد، یک `sortVal` شمسی خام بود (سال×۱۰۰+ماه، مثلاً ۱۴۰۴۱۲) — در حالی که ستون `date` در schema فاز ۱ میلادی/ISO است، پس `WHERE month = ?` روی آن بی‌معنی می‌بود. این ریزمرحله این را با یک تابع جدید حل کرد:
```js
function monthSortValToIsoRange(sortVal){
  // sortVal شمسی → {start, end} میلادی/ISO، با jalaliToGregorian موجود در فایل
}
```
و `fetchInvoicesPage`/`fetchInvoiceStatusCounts` (که در ۲.۲/۲.۴ نوشته شده بودند) برای استفاده از آن به‌روزرسانی شدند — یعنی امضای `InvoiceRepo.getPage`/`getStatusCounts` از این پس `{ monthStart, monthEnd, ... }` می‌گیرد، نه `{ month, ... }` خام. این یک تغییر نسبت به امضای مستندشده در فاز ۱ است که **باید هنگام نوشتن `src/db/InvoiceRepo.js` واقعی اعمال شود.**

**پیاده‌سازی سوییچر ماه:** `renderInvMonthSwitcher` مثل `renderInvStatusCounts` (۲.۴) به یک الگوی async-با-seq-guard تبدیل شد — برچسب ماه/روز بلافاصله (سینک) به‌روز می‌شود، اما عدد «N فاکتور» زیرش از تابع پل جدید `fetchInvMonthCount(monthSortVal, exactDate)` می‌آید که اگر `InvoiceRepo.getMonthCount` در دسترس باشد از آن استفاده می‌کند (با همان تبدیل `monthSortValToIsoRange`)، وگرنه به همان فیلتر دستی قبلی روی آرایه‌ی `invoices` برمی‌گردد. `InvoiceRepo.getMonthCount({monthStart,monthEnd,exactDate}) → عدد` متدی است که در فاز ۱ مستند نشده بود — درست مثل `save`/`remove` که در ۲.۳ کشف شد باید اضافه شوند. ✅ **به‌روزرسانی نسخه ۴.۲:** این متد (به‌همراه `getAllDates`) اکنون واقعاً داخل `src/db/InvoiceRepo.js` ادغام و با SQLite واقعی (node:sqlite) تست شد — نه فقط تست دودی روی یک فایل مستقل. یک شماره‌ی نسخه (`_invMonthSwitcherSeq`) پاسخ دیرآمده‌ی یک کلیک prev/next قبلی را نادیده می‌گیرد — همان الگوی `seq` که در `createVirtualList`/`renderCustInvoicesModal`/`renderInvStatusCounts` استفاده شده.

**`buildInvMonthList` — یادداشت صداقت‌آمیز:** بررسی کد نشان داد این تابع در UI فعلی (سوییچر پیکان قبلی/بعدی + تقویم چرخ‌دار شمسی) **هیچ‌جا صدا زده نمی‌شود** — به نظر می‌رسد برای یک انتخاب‌گر «فهرست کامل ماه‌ها»ی آینده از قبل آماده شده بوده. چون نقشه‌راه صریحاً این دو تابع را با هم در یک ردیف نام برده بود، `buildInvMonthList` هم به همان الگوی Repo/fallback migrate شد (با تابع کمکی جدید `fetchInvMonthList`) تا هر وقت به UI وصل شد از قبل آماده باشد — اما بدون اینکه در جایی صدا زده شود، این تغییر فعلاً بی‌اثر بر رفتار قابل‌مشاهده‌ی اپ است. نکته‌ی طراحی: چون تقویم شمسی در SQLite بومی نیست، گروه‌بندی ماهانه همچنان در جاوااسکریپت انجام می‌شود؛ تنها چیزی که (در صورت وجود Repo) از دیتابیس کشیده می‌شود، فقط ستون سبک `date` است (`InvoiceRepo.getAllDates()`)، نه کل رکورد فاکتور با تمام `items` و فیلدهای دیگرش؛ این کاهش حجم انتقال‌داده همان چیزی است که نقشه‌راه اصلی برای این ریزمرحله در نظر داشت، حتی اگر خودِ گروه‌بندی شمسی در JS باقی بماند. ✅ **به‌روزرسانی نسخه ۴.۲:** `getAllDates()` اکنون واقعاً داخل `src/db/InvoiceRepo.js` ادغام و با SQLite واقعی (node:sqlite) تست شد (خروجی: آرایه‌ای از رشته‌های تاریخ ISO، مرتب‌شده نزولی — تست `getAllDates sorted DESC` را نگاه کنید).

**معیار پذیرش فاز (کلی):** اسکرول یک لیست ۱ میلیونی، فریم‌ریت پایدار (بدون jank محسوس)، حداکثر ~۳۰ DOM node فعال در هر لحظه.

**معیار پذیرش هر ریزمرحله (جزئی‌تر):**
- ۲.۱: اپ بدون خطا در کنسول باز می‌شود؛ `migrateFromIndexedDB.migrate()` یک‌بار با موفقیت اجرا و نتیجه‌اش لاگ می‌شود؛ رفتار فعلی اپ (همه‌چیز با آرایه‌ی حافظه) هیچ تغییری نمی‌کند — *(هنوز روی Capacitor واقعی تست نشده، فقط سینتکس بررسی شده)*
- ۲.۲: تب فاکتورها با ۱۰,۰۰۰+ رکورد تست بدون لگ اسکرول می‌شود؛ فیلترهای موجود (ماه/وضعیت/مشتری/جستجو) دقیقاً همان نتیجه‌ی قبلی را می‌دهند — *(کد نوشته شد و منطق windowing با شبیه‌سازی عددی [بدون DOM/مرورگر] صحت‌سنجی شد؛ تست واقعی روی مرورگر/دستگاه با ۱۰,۰۰۰+ رکورد هنوز انجام نشده — این ردیف باز است تا کسی با دسترسی به مرورگر واقعی آن را تأیید کند)*
- ۲.۳: عملیات (ارسال شد/تسویه/حذف/ویرایش) روی یک فاکتور، دقیقاً همان رفتار قبلی را با تراکنش SQLite تکرار می‌کند، بدون نیاز به بارگذاری کل آرایه — *(کد گلوگاه‌های `dbSaveInvoice`/`dbDeleteInvoiceRecord` نوشته و با `node --check` تأیید شد؛ `InvoiceRepo.save`/`remove` اکنون واقعاً در `src/db/InvoiceRepo.js` ادغام و با SQLite واقعی (node:sqlite) تست شدند [نسخه ۴.۲] — باقی مانده فقط اجرای این ماژول‌ها داخل یک build واقعی Capacitor روی مرورگر/دستگاه است، نه نوشتن کد بیشتر)*
- ۲.۴: مودال فاکتورهای مشتری و شمارش دکمه‌های وضعیت، با تعداد فاکتور بالا (۱۰۰,۰۰۰+) هنوز سریع باز/به‌روز می‌شوند — *(کد نوشته و با `node --check` + شبیه‌سازی عددی fallback تأیید شد؛ `InvoiceRepo.getPage`/`getStatusCounts` اکنون در `src/db/InvoiceRepo.js` واقعی نوشته و با SQLite واقعی (node:sqlite) تست شدند [نسخه ۴.۲] — تست کارایی مستقل روی ۲۰۰,۰۰۰ فاکتور فیک هم ۱۴ms برای فیلتر ترکیبی داد؛ تست واقعی روی مرورگر/Capacitor با ۱۰۰,۰۰۰+ فاکتور واقعی هنوز انجام نشده)*
- ۲.۵: سوییچر ماه شمسی، با تعداد فاکتور بالا (۱۰۰,۰۰۰+) هنوز سریع باز می‌شود — *(کد نوشته و با `node --check` تأیید شد؛ `InvoiceRepo.getMonthCount`/`getAllDates` اکنون واقعاً در `src/db/InvoiceRepo.js` ادغام و با SQLite واقعی تست شدند [نسخه ۴.۲] — تست واقعی روی مرورگر/دستگاه با ۱۰۰,۰۰۰+ فاکتور هنوز انجام نشده)*
- ۲.۶: بعد از حذف آرایه‌ی قدیمی، اپ هیچ رفتار متفاوتی نسبت به قبل از حذف نشان نمی‌دهد (تست رگرسیون کامل روی هر تب)

---

### فاز ۳ — جستجوی متنی مقیاس‌پذیر
**چرا:** `LIKE '%نام%'` بدون ایندکس روی میلیون رکورد فاجعه‌ی کارایی است.

**اقدامات:**

1. جستجوی «شروع با» (اکثر موارد واقعی): ایندکس معمولی + `LIKE 'علی%'` (از ایندکس بهره می‌برد)
2. جستجوی کامل داخل متن: جدول FTS5
```sql
CREATE VIRTUAL TABLE customers_fts USING fts5(name, company, content='customers', content_rowid='id');
```
```sql
SELECT * FROM customers_fts WHERE customers_fts MATCH 'علی*' LIMIT 30;
```
3. حفظ debounce فعلی (۱۲۰ms) روی ورودی جستجو

**معیار پذیرش فاز:** جستجوی هر عبارت روی ۱ میلیون مشتری، زیر ۳۰ میلی‌ثانیه.

---

### فاز ۴ — محاسبات آماری بدون فریز UI
**چرا:** حلقه‌ی JS فعلی روی کل `invoices` برای تب آمار، با میلیون‌ها رکورد چند ثانیه طول می‌کشد و صفحه را قفل می‌کند.

**اقدامات:**

1. انتقال تمام Aggregation به SQL:
```sql
SELECT strftime('%Y-%m', date) AS month,
       SUM(json_extract(data,'$.totalProfit')) AS profit
FROM invoices
WHERE date BETWEEN ? AND ?
GROUP BY month;
```
2. هر منطق باقی‌مانده که باید در JS اجرا شود → داخل **Web Worker** جدا (thread اصلی هرگز قفل نشود)

**معیار پذیرش فاز:** بازکردن تب آمار با ۵ میلیون فاکتور، بدون افت فریم‌ریت محسوس در UI اصلی.

---

### فاز ۵ — یکپارچگی نوشتن (Write Integrity)
**چرا:** ثبت فاکتور + کسر موجودی باید یک عملیات atomic باشد.

**اقدامات:**

1. هر عملیات چندمرحله‌ای در یک `TRANSACTION`:
```sql
BEGIN TRANSACTION;
UPDATE inventory SET qty = qty - ? WHERE id = ?;
INSERT INTO invoices (...) VALUES (...);
COMMIT;
```
2. Batch insert برای Restore بکاپ (`executeSet` به‌جای هزاران `INSERT` جدا)

**معیار پذیرش فاز:** شبیه‌سازی کرش وسط عملیات → داده هرگز نیمه‌کاره باقی نماند (rollback خودکار).

---

### فاز ۶ — Backup/Restore مقیاس‌پذیر
**چرا:** JSON کامل با میلیون‌ها رکورد چند صد مگابایت می‌شود؛ پارس آن کند و کرش‌آور است.

**اقدامات:**

1. export/import مستقیم فایل دیتابیس SQLite (`.db`) — کپی فایل خام، نه serialize/parse JSON
2. برای export انسانی (اکسل و مشابه)، خروجی streaming و صفحه‌بندی‌شده باشد، نه یکجا در حافظه

**معیار پذیرش فاز:** بکاپ/بازیابی ۱۰ میلیون رکورد بدون افزایش غیرمنطقی مصرف RAM.

---

### فاز ۷ — تست فشار و اعتبارسنجی نهایی (Stress Test)

**اقدامات:**

1. اسکریپت seed: تولید ۱ میلیون مشتری + ۵ میلیون فاکتور فیک (batch insert)
2. سناریوهای تست:
   - باز کردن تب فاکتورها
   - اسکرول تا انتهای لیست
   - جستجوی نام مشتری
   - فیلتر ترکیبی ماه + وضعیت + مشتری
   - بازکردن تب آمار
3. آستانه‌ی پذیرش: هر عملیات فوق زیر ۱۰۰ میلی‌ثانیه پاسخ دهد
4. بررسی با Performance Profiler مرورگر برای شناسایی گلوگاه‌های باقی‌مانده

---

## ۵. معیارهای موفقیت (Definition of Done)

| معیار | آستانه هدف |
|---|---|
| زمان پاسخ فیلتر ترکیبی (ماه+وضعیت+مشتری) روی ۱M ردیف | < ۵۰ms |
| زمان جستجوی نام روی ۱M مشتری | < ۳۰ms |
| تعداد DOM node فعال هنگام اسکرول لیست بزرگ | < ۳۰ |
| زمان بازکردن تب آمار با ۵M فاکتور | بدون فریز محسوس UI |
| مصرف RAM هنگام کار با دیتاست بزرگ | ثابت (مستقل از تعداد کل رکورد) |
| یکپارچگی داده پس از کرش شبیه‌سازی‌شده | بدون رکورد نیمه‌کاره |

---

## ۶. ریسک‌ها و راهکار مقابله

| ریسک | تأثیر | راهکار | وضعیت |
|---|---|---|---|
| مهاجرت داده‌ی موجود از IndexedDB به SQLite با خطا مواجه شود | از دست رفتن داده‌ی فعلی | اسکریپت migration جدا + اجرای آن روی یک کپی، تأیید صحت قبل از حذف داده‌ی قدیم | ✅ پیاده‌سازی شده در `migrateFromIndexedDB.js` (شمارش تطبیقی قبل از تأیید)؛ در نسخه‌ی ۴.۲ با یک منبع داده‌ی fake (۱۲۰۰ مشتری/۲۶۰۰ فاکتور) روی SQLite واقعی (node:sqlite) تست شد و صحیح بود — هنوز روی IndexedDB واقعیِ یک مرورگر/دستگاه تست نشده |
| فاکتورهایی که به یک `customerId` حذف‌شده (مشتری قبلاً پاک‌شده) اشاره می‌کنند، مهاجرت را با خطای FOREIGN KEY constraint می‌شکنند | مهاجرت با خطا متوقف می‌شود و نه فاکتور یتیم و نه بقیه‌ی همان batch نوشته می‌شوند (rollback تراکنشی) | برای هر `customerId` یتیم، قبل از نوشتن فاکتورها یک ردیف جایگزین («مشتری حذف‌شده») در جدول customers ساخته شود | ✅ کشف و رفع شد در نسخه‌ی ۴.۲ — حین تست ادغام واقعی (نه یک ریسک نظری؛ یک تست assertion مشخص هم برایش نوشته شد: `testOrphanCustomerMigration`) |
| `node:sqlite` (که برای تست منطق SQL در این محیط استفاده شد) از نظر رفتار با پلاگین واقعی `@capacitor-community/sqlite` می‌تواند در جزئیات (نوع بازگشتی، رفتار تراکنش تودرتو، محدودیت‌های FTS5/JSON1) فرق داشته باشد | تست‌های سبز در Node لزوماً یعنی روی دستگاه واقعی هم درست کار می‌کند، ولی تضمین ۱۰۰٪ نیست | بعد از اولین build موفق Capacitor (بخش ۸)، همان سناریوهای `tests/run-integration-tests.mjs` را — این‌بار روی خودِ دستگاه/شبیه‌ساز — دستی یا با یک تست E2E تکرار کنید | 🆕 ریسک جدید — به‌خاطر روش تست نسخه‌ی ۴.۲ اضافه شد؛ اقدامی روی آن انجام نشده |
| این sandbox توسعه نه باینری مرورگر (Chromium/Chrome) نصب‌شده دارد و نه دسترسی شبکه برای نصب آن (حتی با ابزارهایی مثل Playwright که از قبل به‌صورت کتابخانه در دسترس است) | معیارهای پذیرش ۲.۱ (باز شدن بدون خطای کنسول)، ۲.۲/۲.۴/۲.۵ (تست واقعی روی مرورگر با ده‌ها هزار رکورد)، ۲.۶ (رگرسیون کامل UI)، و مهاجرت روی IndexedDB واقعی مرورگر (نه منبع تزریق‌شده‌ی fake) قابل اجرا در این محیط نیستند — نه به‌خاطر نبودن کد، بلکه صرفاً نبود ابزار اجرا | این تست‌ها باید در محیطی با دسترسی به مرورگر واقعی (لپ‌تاپ/سرور CI با Playwright/Chromium نصب‌شده، یا خودِ Android/iOS از طریق Capacitor) اجرا شوند؛ تا آن زمان، `tests/run-integration-tests.mjs` و `tests/perf-test.mjs` (هر دو Node-only) تنها راستی‌آزمایی خودکار موجودند | 🆕 ریسک جدید — در نسخه‌ی ۴.۳ حین تلاش عملی برای اجرای تست Playwright کشف شد (نه یک ریسک نظری؛ `npx playwright install chromium` با خطای دسترسی شبکه/npm config شکست خورد و هیچ باینری مرورگر از قبل روی سیستم پیدا نشد) |
| کدهای فعلی مستقیماً با آرایه‌ی حافظه کار می‌کنند (نه Repository) | نیاز به بازنویسی گسترده در فاز‌های بعد | در فاز ۱، Repository را دقیقاً با همان امضای توابع فعلی هم‌راستا طراحی کن | ✅ امضای توابع (`getPage`, `getStatusCounts`, ...) عمداً نزدیک به منطق `renderInvoicesList`/`renderInvStatusCounts` فعلی طراحی شد |
| ~~`InvoiceRepo.getStatusCounts` روی ۱M فاکتور (نه ۲۰۰k) به‌طور پایدار ۴۱–۴۵ms طول می‌کشد — خیلی نزدیک به آستانه‌ی پذیرش ۵۰ms~~ | با اضافه‌شدن IPC واقعی پلاگین `@capacitor-community/sqlite` روی دستگاه یا با رشد بیشتر از ۱M رکورد، این query می‌توانست از آستانه رد شود | راهکار محتمل ثبت‌شده در نسخه‌ی ۴.۴: ایندکس پوششی (`covering index`) روی `(date, status, sent)` | ✅ **حل شد در نسخه‌ی ۴.۵ (سطح SQL/ایندکس):** ایندکس `idx_inv_date_status_sent ON invoices(date, status, sent)` به `schema.js` اضافه شد؛ ۳ اجرای مستقل `perf-test-1m.mjs` بعد از این تغییر، `getStatusCounts` را در **۹–۱۰ms** نشان داد (قبلاً ۴۱–۴۵ms) — بیش از ۴ برابر بهبود، اکنون با فاصله‌ی معنادار از آستانه. ۱۸ assertion موجود بدون رگرسیون پاس شدند. ✅ **بند ⓑ در نسخه‌ی ۴.۶ بسته شد:** `tests/single-write-cost-test.mjs` هزینه‌ی خالص این ایندکس روی نوشتن تکی را با مقایسه‌ی دو schema (با/بدون آن) روی ۱M ردیف seed‌شده اندازه گرفت — هزینه‌ی اضافه‌ی هر `INSERT`/`UPDATE` تکی حدود **۰.۰۱–۰.۰۲ms** (زیر‌میلی‌ثانیه، عملاً ناچیز) بود، با ۳ اجرای مستقل تکرارپذیر. **هنوز باز:** ⓐ تأیید روی `@capacitor-community/sqlite` واقعی روی دستگاه (مثل بقیه‌ی موارد فاز ۱ — سربار IPC واقعی روی دستگاه توسط هیچ‌کدام از این تست‌های node:sqlite اندازه‌گیری نمی‌شود) |
| کتابخانه‌ی Virtual List با RTL/فارسی رفتار نادرست داشته باشد | چیدمان به‌هم‌ریخته در UI | به‌جای کتابخانه‌ی خارجی، پیاده‌سازی دستی (`createVirtualList` در `4.html`) انجام شد؛ چیدمان با document flow معمولی (نه absolute-position) کار می‌کند که ریسک به‌هم‌ریختگی RTL را کم می‌کند، اما هنوز روی نمونه‌ی واقعی فارسی/RTL در مرورگر تست نشده | 🟡 پیاده‌سازی شد (ریزمرحله ۲.۲) — تست بصری واقعی باقی مانده |
| حجم فایل دیتابیس روی گوشی‌های حافظه‌محدود مشکل ایجاد کند | کرش روی دستگاه‌های ضعیف | تعیین سقف واقعی بر اساس فضای ذخیره‌سازی هدف در فاز ۰ | 🟡 **عدد اولیه در نسخه‌ی ۴.۷ اندازه‌گیری شد:** `tests/db-file-size-test.mjs` یک دیتابیس فایل‌محور واقعی (نه `:memory:`) با همان schema کامل ساخت و حجم را با `fs.statSync` در ۳ نقطه‌ی رشد اندازه گرفت — ۱M فاکتور + ۱۵k مشتری بعد از `VACUUM` = **~۲۸۰ مگابایت** (~۲۹۴ بایت/فاکتور، رشد خطی). این عدد بدون داده‌ی واقعی `items[]` بلند و بدون سیاست VACUUM دوره‌ای است. ✅ **بند VACUUM دوره‌ای در نسخه‌ی ۴.۸ با عدد بسته شد:** `tests/vacuum-churn-test.mjs` یک سناریوی churn واقعی (حذف+درج، نه insert خالص) روی دو دیتابیس فایلی جدا اجرا کرد — بدون VACUUM بعد از ۱۰ دور churn (۵٪/دور روی ۵۰۰k رکورد) حجم فایل به ۱.۲۴x پایه رسید (+۴۶.۹۱MB بادکردگی خالص، بدون افزایش تعداد ردیف واقعی)؛ با VACUUM بعد از هر دور، حجم حتی از پایه هم کوچک‌تر ماند (۰.۹۳x) ولی هر اجرای VACUUM ~۱۷۸۲ms روی ۵۰۰k رکورد طول کشید و دیتابیس را حین اجرا قفل می‌کند. توصیه‌ی عملی (نه تصمیم نهایی): VACUUM به‌عنوان maintenance job کم‌تکرار (شبانه/هفتگی) در فاز ۶ یا یک فاز نگهداری جدا، نه synchronous بعد از هر تراکنش. **هنوز باز:** تأیید هر دو عدد (bloat و هزینه‌ی زمانی VACUUM) روی فایل‌سیستم/CPU واقعی دستگاه از طریق پلاگین `@capacitor-community/sqlite` (مثل بقیه‌ی موارد فاز ۱) |
| build روی GitHub Actions شکست بخورد (نسخه‌ی ناسازگار پکیج، SDK، و...) | نمی‌توان APK قابل‌نصب گرفت | لاگ کامل هر اجرای Actions بررسی شود؛ نسخه‌ی پکیج‌ها (`@capacitor/*`, `@capacitor-community/sqlite`) پین شده نگه داشته شود تا آپدیت خودکار باعث شکست ناگهانی نشود | 🆕 ریسک جدید — به‌خاطر تغییر مسیر build (بخش ۸) اضافه شد |
| APK خروجی GitHub Actions نسخه‌ی debug است، نه release امضاشده | قابل نصب برای تست شخصی هست ولی برای انتشار در Google Play کافی نیست | یک workflow جدا (یا مرحله‌ی اضافه در همین workflow) برای build نسخه‌ی release با keystore واقعی، جدا از این فاز و بعد از تکمیل فازهای عملکردی | 🆕 یادداشت شد — اقدام لازم نیست تا زمانی که انتشار رسمی مدنظر باشد |
| بازیابی بکاپ (`doRestore`) یا پاک‌کردن کامل (`clearAllData`) فقط IndexedDB را تغییر می‌دهد، نه SQLite | اگر SQLite فعال باشد، بعد از بازیابی بکاپ، دو منبع داده تا مهاجرت بعدی ناهمگام می‌مانند | عمداً به فاز ۵ موکول شد («Batch insert برای Restore بکاپ» از قبل در فاز ۵ برنامه‌ریزی شده بود) — تا آن زمان کاربرانی که همزمان از build مبتنی بر Capacitor/SQLite استفاده می‌کنند، بعد از بازیابی بکاپ باید از مسیر مهاجرت دوباره عبور کنند | 🆕 در حین پیاده‌سازی ۲.۳ کشف شد — هنوز اقدامی روی آن انجام نشده |
| پارامتر «ماه» که `fetchInvoicesPage`/`fetchInvoiceStatusCounts` به `InvoiceRepo` می‌فرستند، مقدار `sortVal` شمسی (سال×۱۰۰+ماه، مثلاً ۱۴۰۴۱۲) است؛ اما ستون `date` در schema فاز ۱ میلادی/ISO است و هیچ ستون یا ایندکس شمسی جداگانه‌ای در schema تعریف نشده | وقتی SQLite واقعاً وصل شود، `InvoiceRepo.getPage`/`getStatusCounts` نمی‌تواند مستقیم `WHERE month = ?` بزند — باید یا `sortVal` را به یک بازه‌ی میلادی (`start`/`end` ISO) تبدیل کند، یا خودِ کد فراخوان (در `4.html`) این تبدیل را قبل از فراخوانی انجام دهد؛ تا این تصمیم گرفته نشود، فیلتر ماه در حالت SQLite واقعی می‌تواند نتیجه‌ی نادرست یا خالی بدهد (فعلاً بی‌اثر است چون `InvoiceRepo` واقعی وجود ندارد و مسیر fallback حافظه — که خودش با `getPersianMonthInfo` روی تاریخ میلادی هر فاکتور محاسبه می‌کند — همیشه اجرا می‌شود) | یا `InvoiceRepo.getPage`/`getStatusCounts` را طوری طراحی کنیم که `month` را به‌صورت `{start, end}` ISO بگیرد (تبدیل شمسی→میلادی همین الان در `4.html` با تابع `jalaliToGregorian` موجود است و به‌سادگی قابل استفاده در فراخوان‌کننده است)، یا مستندات فاز ۱ را به‌صراحت به‌روزرسانی کنیم که `month` یک `sortVal` شمسی است و Repository باید خودش تبدیل را انجام دهد — این تصمیم باید قبل از تست میدانی واقعی روی SQLite (نه فقط سینتکس) گرفته شود | ✅ در ریزمرحله‌ی ۲.۵ حل شد: تابع `monthSortValToIsoRange(sortVal)` اضافه شد که `sortVal` شمسی را به `{start,end}` میلادی/ISO تبدیل می‌کند؛ `fetchInvoicesPage` و `fetchInvoiceStatusCounts` (۲.۲/۲.۴) برای فرستادن `monthStart`/`monthEnd` به‌جای `month` خام به‌روزرسانی شدند — یعنی گزینه‌ی اول پیشنهادی همین ردیف («`InvoiceRepo` را طوری طراحی کنیم که `month` را به‌صورت `{start,end}` ISO بگیرد») انتخاب شد. این یعنی امضای `InvoiceRepo.getPage`/`getStatusCounts` نسبت به آنچه در فاز ۱ مستند شده بود تغییر کرده و باید هنگام نوشتن `src/db/InvoiceRepo.js` واقعی رعایت شود. هنوز روی SQLite واقعی تست نشده (چون Repo واقعی وجود ندارد)، اما دیگر یک تصمیم معماری معلق نیست |

---

## ۷. ترتیب اجرای پیشنهادی و وابستگی فازها

```
فاز ۰ (baseline)
   │
   ▼
فاز ۱ (SQLite)  ──── پیش‌نیاز اجباری برای همه‌ی فازهای بعد          [🟡 اسکلت آماده]
   │
   ▼
فاز ۲ (Virtual List)  ──── بدون این، فاز ۱ در عمل بی‌اثر می‌ماند (گلوگاه رندر)
   │
   ├──▶ فاز ۳ (جستجو)      ┐
   │                        ├─ می‌توانند موازی اجرا شوند
   └──▶ فاز ۴ (آمار)        ┘
   │
   ▼
فاز ۵ (یکپارچگی نوشتن)
   │
   ▼
فاز ۶ (Backup/Restore)
   │
   ▼
فاز ۷ (تست فشار نهایی) ──── تأیید نهایی قبل از استفاده‌ی واقعی
```

**نکته:** فاز ۱ و ۲ باید همیشه با هم دیده شوند — پیاده‌سازی SQLite بدون Virtual List فقط نصف مشکل را حل می‌کند. به همین دلیل، اتصال واقعی Repository ها به `4.html` (آخرین مورد باقی‌مانده از فاز ۱) عمداً به‌عنوان قدم اول فاز ۲ انجام می‌شود، نه به‌عنوان یک قدم جدا قبل از آن.

---

## ۸. زیرساخت Build (تصمیم جدید نسبت به نسخه‌ی ۲.۰)

**تصمیم:** به‌جای نصب Android Studio/Xcode روی سیستم توسعه، از GitHub Actions
برای build خودکار APK استفاده می‌شود. این تصمیم چرخه‌ی فازهای بالا را تغییر
نمی‌دهد؛ فقط مکانیزم "چطور یک نسخه‌ی قابل‌نصب می‌گیریم" را تغییر می‌دهد.

**زیرساخت آماده‌شده:**
- `.github/workflows/build-android.yml` — با هر پوش به `main` (یا اجرای دستی)، خودکار:
  ۱) Node.js + Java 17 + Android SDK را نصب می‌کند
  ۲) `npm install` و `npx cap sync android` را اجرا می‌کند
  ۳) `./gradlew assembleDebug` را می‌سازد
  ۴) فایل `app-debug.apk` را به‌عنوان Artifact قابل‌دانلود (نگه‌داری ۳۰ روزه) آپلود می‌کند
- `.gitignore` — پوشه‌های `android/`, `ios/`, `node_modules/` را از ریپو دور نگه می‌دارد چون Actions هر بار از صفر می‌سازدشان
- `BUILD-VIA-GITHUB.md` — راهنمای کامل کاربر: ساخت ریپو → پوش → دیدن اجرای Actions → دانلود APK از Artifacts

**پیامد روی فازهای بعدی:**
- هر فازی که تغییری در کد اندروید/پلاگین‌های native ایجاد کند (فاز ۱ به بعد)، معیار پذیرش «build سالم روی GitHub Actions» را هم باید رد کند، علاوه بر معیار عملکردی خودش
- تست فشار فاز ۷ (seed ۱ میلیون رکورد) باید داخل خود اپ روی دستگاه واقعی انجام شود، نه در محیط Actions (چون Actions فقط build می‌کند، نه اجرای runtime روی دستگاه)
- برای انتشار نهایی در Google Play (خارج از محدوده‌ی این نقشه راه)، یک مرحله‌ی جدا برای ساخت keystore واقعی و افزودن مرحله‌ی `assembleRelease` امضاشده به workflow لازم است — این به‌صورت آگاهانه خارج از دامنه‌ی فازهای ۰ تا ۷ نگه داشته شده
