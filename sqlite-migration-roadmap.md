# نقشه‌راه ۳: حذف کامل `customers[]` از حافظه (SQLite تنها منبع کامل)

## زمینه (خلاصه، برای شروع تازه بدون نیاز به نقشه‌ی قبلی)

نقشه‌راه ۲ (فازهای ۰ تا ۱۱) تمام شد: `invoices[]` کامل از `html8.html` حذف شد و فقط یک کش لِیزی صفحه‌بندی‌شده (`window.__invoiceCache`) باقی ماند که مستقیم از `InvoiceRepo` (SQLite) پر می‌شود.

`customers[]` عمداً از آن نقشه‌راه کنار گذاشته شد چون یک الگوی متفاوت دارد: بر خلاف فاکتورها، مشتری‌ها **هیچ‌جای فایل lazy-load نمی‌شوند** — هر بار که برنامه بالا می‌آید یا بازیابی می‌شود، *کل* لیست مشتریان یک‌جا از `CustomerRepo` خوانده و در آرایه‌ی سراسری `customers` ریخته می‌شود (`fetchAllCustomersForStartup`). نوشتن/خواندنِ *اصلی* مشتری از قبل کاملاً روی SQLite است (`CustomerRepo`) — این نقشه درباره‌ی *وصل‌کردن* مشتری‌ها به SQLite نیست (از فاز ۰-۶ قبلی همین‌طوره)، بلکه درباره‌ی حذف کپیِ کاملِ حافظه‌ای آن‌هاست، دقیقاً همان کاری که نقشه‌راه ۲ برای فاکتورها انجام داد.

## هدف

آرایه‌ی سراسری `let customers=[]` (خط ۶۱۵ فعلی) کامل از `html8.html` حذف شود. تمام رندر/جستجو/شمارش مشتری باید یا از `CustomerRepo` (SQLite) مستقیم بخواند، یا از یک کش لِیزی مشابه `__invoiceCache` (نه یک کپی کامل).

## چرا فازبندی؟

`customers` علاوه بر رندر لیست، در نقاط دیگری هم استفاده می‌شود که رفتار متفاوتی دارند: چک «آیا اصلاً مشتری‌ای هست» (seed داده‌ی نمونه)، مسیرهای بازیابی بکاپ (legacy JSON و ndjson)، و `clearAllData`. هر کدام باید جدا و قابل تست دستی روی گوشی تبدیل شوند — دقیقاً با همان احتیاط نقشه‌راه ۲ (این فایل تست خودکار ندارد؛ `tests/` فقط `CustomerRepo` را مستقیم تست می‌کند، نه خودِ `html8.html`).

**خارج از دامنه:** `inventory`/`productionQueue`/`vanDrivers` (کنار همین خط تعریف) دست‌نخورده می‌مانند — به SQLite migrate نشده‌اند و جزو هدف این نقشه نیستند.

---

### فاز ۱ — دسته‌بندی دقیق و خط‌به‌خط (پیش‌نیاز، بدون تغییر کد)
- [ ] هر رخداد واقعی `customers` (نه کامنت/رشته/`customerId`/`renderCustomersList` و مشتقاتش) در `html8.html` را با شماره‌خط در `customers-array-checklist.md` ثبت کن: خط، نوع (خواندن/نوشتن)، تابع، دسته (نوشتن‌آینه‌ای / fallback ناامن / کش لِیزی مجاز / تعریف state).
  نقاط شناخته‌شده‌ی فعلی که باید در این چک‌لیست دقیق شوند (لیست شروع، نه لیست نهایی):
  - خط ۶۱۵: تعریف `let customers=[]`
  - خط ۱۱۵۲, ۱۱۷۲: دو مسیر `loadState` (عادی + fallback نهایی)
  - خط ۱۲۳۴-۱۲۳۶: `seedSampleDataIfEmpty` (چک طول + push داده‌ی نمونه)
  - خط ۳۳۸۵: `renderCustomersList` (چک `!customers.length` برای پیام خالی)
  - خط ۶۲۶۰, ۶۳۴۲, ۶۳۷۹: `doRestoreLegacyJson` / `doRestoreNdjson` / `clearAllData`
- [ ] اسکریپت `memory-array-audit.mjs` موجود را برای پوشش `customers` هم به‌روز کن (اگر از قبل نیست) تا بشود بعد از هر فاز عدد را با سقف پیش‌بینی‌شده مقایسه کرد — به همان روش `PHASE_EXPECTATIONS` که برای `invoices` استفاده شد.

### فاز ۲ — ساخت کش لِیزی مشتری (`window.__customerCache`)
- [ ] یک کش لِیزی جدید بساز (الگوی دقیق `window.__invoiceCache` را کپی کن، نه از صفر طراحی کن): `get(id)`, `setMany(list)`, `invalidate()`. هدف: نقاطی که فعلاً یک آیتم را از `customers.find(...)` می‌گیرند (مثلاً `resolveCustomerById` اگر مشابهش برای مشتری لازم شود) بتوانند بدون نگه‌داشتن کل آرایه کار کنند.
- [ ] `getCustomerById`/`findCustomerByName` (که از قبل async و روی `CustomerRepo` هستند طبق نقشه‌راه ۲) را بررسی کن — اگر همین الان کافی‌اند و مصرف‌کننده‌ای از `customers[]` مستقیم برای lookup تکی استفاده نمی‌کند، این فاز ممکن است لازم نباشد؛ فقط در صورت پیدا شدن یک مصرف واقعی در فاز ۱ اضافه کن (YAGNI — کش الکی نساز).

### فاز ۳ — حذف چک «آیا مشتری‌ای هست» از seed
- [ ] `seedSampleDataIfEmpty`: `if(!customers.length)` باید به یک چک async روی `CustomerRepo` تبدیل شود (مثلاً `await window.__CustomerRepo.count()===0` یا معادلش — بررسی کن چه متدی از قبل روی `CustomerRepo` هست، متد جدید اضافه نکن مگر لازم باشد).
- [ ] چون `seedSampleDataIfEmpty` الان sync است و هر فراخوان‌کننده‌اش (خط ۱۱۸۰ در `loadState`) هم باید `async`/`await` شود، این زنجیره را کامل دنبال کن تا جایی که به یک نقطه‌ی از قبل async برسی (خودِ `loadState` که از فاز‌های قبلی async هست، پس این زنجیره کوتاه است).
- [ ] `customers.push(sampleCust)` حذف شود؛ فقط `dbSaveCustomer(sampleCust)` بماند (که از قبل مستقل نوشتن SQLite را انجام می‌دهد).
- تست دستی روی گوشی لازم: پاک‌کردن کامل داده و باز کردن اپ تازه، دیدن مشتری نمونه.

### فاز ۴ — حذف چک خالی‌بودن در `renderCustomersList`
- [ ] خط `if(!customers.length){...return;}` در `renderCustomersList` باید حذف یا با یک چک روی نتیجه‌ی `fetchCustomersPage` جایگزین شود (تابع از قبل `fetchCustomersPage(0,custRenderLimit,q)` را صدا می‌زند و `total`/`list.length` را دارد — این پیام «خالی» را می‌شود از همان‌جا تشخیص داد، نیازی به خواندن آرایه‌ی سراسری نیست).
- تست دستی: تب مشتریان با ۰ مشتری، با ۱ مشتری، با جستجوی بدون‌نتیجه.

### فاز ۵ — مسیرهای بازیابی/پاک‌سازی (پرریسک‌ترین فاز)
برای هر سه مسیر (`loadState` هر دو شاخه، `doRestoreLegacyJson`, `doRestoreNdjson`, `clearAllData`):
- [ ] خط `customers=await fetchAllCustomersForStartup()` (یا معادل `legacyCustomers`) حذف شود. به‌جایش: اگر رندر بعدی این مسیر فقط `renderCustomersList()` صدا می‌زند (که خودش `fetchCustomersPage` می‌زند)، نیازی به هیچ متغیر واسط نیست — فقط ترتیب await‌ها را حفظ کن (نوشتن در SQLite باید قبل از رندر کامل شده باشد).
- [ ] `loadState` مسیر `catch` نهایی (خط ۱۱۷۲, `customers=legacyCustomers`): این مسیر وقتی SQLite کلاً در دسترس نیست فراخوانی می‌شود — بررسی کن آیا واقعاً چیزی برای رندر می‌ماند یا باید مثل فاکتورها پیام «SQLite در دسترس نیست» نشان داده شود؛ این فرق دارد با بقیه‌ی موارد چون اینجا اصلاً `CustomerRepo` کار نمی‌کند.
- تست دستی اجباری روی گوشی (نه فقط syntax check): بازیابی بکاپ legacy JSON، بازیابی ndjson، `clearAllData` — هر سه با تعداد نسبتاً زیاد مشتری، دیدن اینکه لیست درست و کامل رندر می‌شود.

### فاز ۶ — حذف نهایی تعریف + رگرسیون کامل
- [ ] `customers` از خط تعریف ۶۱۵ حذف شود (`let inventory=[], productionQueue=[], vanDrivers=[];`).
- [ ] `grep -n '\bcustomers\b' html8.html` روی کل فایل — تأیید کن هیچ خواننده/نویسنده‌ی واقعی (نه کامنت/رشته) باقی نمانده.
- [ ] `node memory-array-audit.mjs` — هر سه بلاک `<script>` سالم.
- [ ] `node --experimental-sqlite tests/run-integration-tests.mjs` و بقیه‌ی تست‌های `tests/` مرتبط با `CustomerRepo` اجرا و سبز شوند.
- [ ] تست دستی کامل روی گوشی: افزودن/ویرایش/حذف مشتری، جستجو، اسکرول بعد از صفحه‌ی اول، بازکردن اپ تازه (seed)، سه مسیر بازیابی/پاک‌سازی فاز ۵ دوباره.

---

## نکته‌ی مهم قبل از شروع فاز ۱

هنوز هیچ فازی از این نقشه اجرا نشده. توصیه می‌شود بعد از فاز ۳ و بعد از فاز ۵ (پرریسک‌ترین‌ها) حتماً روی گوشی امتحان کنی، نه اینکه همه‌ی فازها پشت‌سرهم بدون تست میانی انجام شوند — همان درسی که از نقشه‌راه ۲ گرفته شد.
