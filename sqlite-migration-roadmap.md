# نقشه‌راه: تبدیل SQLite به تنها منبع حقیقت (Single Source of Truth)

## خلاصه‌ی وضعیت قبلی (فازهای ۰ تا ۶ — همگی ✅ کامل شدند، جزئیات از این نقشه حذف شد)

فازهای ۰ تا ۶ (تثبیت baseline، سخت‌کردن migration، حذف خواندن‌های `invoices[]`/`customers[]`، تبدیل بکاپ/ریستور، حذف کامل idb helperها + خودِ دیتابیس IndexedDB روی دستگاه، و تست رگرسیون نهایی) همگی با موفقیت انجام شدند. نتیجه‌ی نهایی آن‌ها:

- `html8.html` دیگر هیچ فراخوانی یا تعریف `idb*` ندارد و لایه‌ی IndexedDB (هم کد و هم خودِ دیتابیس روی دستگاه) کامل حذف شده.
- `InvoiceRepo`/`CustomerRepo` (SQLite) تنها منبع نوشتن/خواندن *اولیه‌ی* فاکتور و مشتری در کل اپ‌اند.
- `migrateFromIndexedDB.js` هنوز وصل و idempotent است و برای مهاجرت یک‌باره‌ی داده‌ی کاربران قدیمی لازم است.
- ۳۰ تست واقعی `tests/` سبزند، ۰ رگرسیون.
- تنها مورد باقی‌مانده از این بخش: به‌روزرسانی `roadmap-infinite-records-v4_28.md`، که در آرشیو فعلی پروژه وجود ندارد.

*(جزئیات کامل هر فاز — چک‌لیست‌ها، باگ‌های پیداشده، شماره‌خط‌ها — در نسخه‌ی قبلی این فایل بود؛ چون همه‌شان ✅ بسته شده بودند، برای خلوت‌شدن نقشه حذف شدند.)*

---

## هدف بعدی (این نقشه از اینجا به بعد فعال است)

حذف کامل خودِ آرایه‌های حافظه `invoices[]`/`customers[]` (نه فقط خواندن‌هایشان). این با فازهای ۰ تا ۶ فرق دارد چون آن‌ها هدفشان «SQLite تنها منبعِ **نوشتن/خواندنِ اولیه**» بود، نه نبودِ **هیچ** آرایه‌ی حافظه‌ای در کل فایل — چند تابع (`resolveInvoiceById`, `renderInvoicesList`) عمداً یک کش لِیزی نگه می‌داشتند که از SQLite پر می‌شود، نه یک کپی مستقل. هنوز هیچ‌کدام از آیتم‌های زیر اجرا نشده.

---

## نقشه‌راه ۲ — حذف کامل `invoices[]`/`customers[]` از حافظه (fail-fast کامل)

**چرا فازبندی، نه یک مرحله؟** بر خلاف حذف idb (که یک لایه‌ی کاملاً مجزا و ۲۴ نقطه‌ی مستقل بود)، این آرایه‌ها توی ~۱۵ تابع مختلف با نقش‌های متفاوت استفاده می‌شوند — رندر لیست (pagination/فیلتر/جستجو)، آمار fallback، بکاپ/ریستور، و کش لِیزی برای کلیک‌های تکی. این فایل (`html8.html`) هیچ محیط تست خودکار ندارد (تست‌های `tests/` فقط روی `InvoiceRepo`/`CustomerRepo` مستقیم کار می‌کنند، نه خودِ این فایل)، پس هر فاز باید کوچک، قابل syntax-check، و قابل توضیح دقیق برای تست دستی توسط خودت روی گوشی باشد.

**نکته‌ی مهم قبل از شروع:** `inventory`/`productionQueue`/`vanDrivers` (خط ۶۰۵) خارج از دامنه‌ی این نقشه‌اند — این‌ها به SQLite migrate نشده‌اند و اصلاً بخشی از هدف اصلی (فاکتور/مشتری) نبودند؛ دست‌نخورده می‌مانند.

### فاز ۷ — دسته‌بندی دقیق و خط‌به‌خط (پیش‌نیاز، بدون تغییر کد) ✅ کامل شد (این جلسه)
- [x] هر ۸۰ رخداد `invoices` و ۴۴ رخداد `customers` در `html8.html` را با شماره‌خط، در یک فایل چک‌لیست جدید (`memory-array-checklist.md`) ثبت شد — با ستون‌های: خط، شناسه، تعداد در آن خط، تابع/بخش، دسته، توضیح. مجموع ۱۲۴ رخداد در ۱۱۴ ردیف، دسته‌بندی‌شده در ۷ گروه (A تا G؛ چهار گروه اصلی نقشه‌راه + سه گروه کمکی برای رشته/کامنت/تعریف‌state که لازم بود چون نه هر رخداد `invoices`/`customers` واقعاً به آرایه‌ی سراسری اشاره می‌کند). جمع‌بندی: ۲۹ رخداد دسته B (fallback ناامن)، ۵ رخداد دسته A (نوشتن‌آینه‌ای)، ۱۴ رخداد دسته C (کش لِیزی، خارج از دامنه‌ی حذف)، بقیه D/E/F/G. یک ناسازگاری واقعی هم پیدا شد: `saveCustomerEdit` (خط ۳۵۸۱) هنوز چک تکراری‌بودن نام را روی آرایه‌ی حافظه انجام می‌دهد، بر خلاف `addCustomer` که در فاز ۳ قبلی به `findCustomerByName` منتقل شده بود — در چک‌لیست ثبت شد تا فاز ۸/۹ تصمیم بگیرد.
- [x] اسکریپت `memory-array-audit.mjs` ساخته شد (`node --check` روی هر سه بلاک `<script>` + شمارش `\binvoices\b`/`\bcustomers\b` هم‌ارز `grep -o`) — با `node memory-array-audit.mjs --phase N` می‌توان بعد از هر فاز بعدی عدد فعلی را با سقف پیش‌بینی‌شده‌ی همان فاز (جدول `PHASE_EXPECTATIONS` داخل اسکریپت) مقایسه کرد. تست شد و در baseline فعلی (فاز ۷) هر سه بلاک سینتکس سالم دارند و اعداد (۸۰/۴۴) دقیقاً با چک‌لیست مطابقت می‌کنند.

### فاز ۸ — حذف fallbackهای ناامن (کم‌ریسک، اول انجام شود) ✅ کامل شد (این جلسه)
موارد شناسایی‌شده که همین الان صراحتاً «سقوط به داده‌ی احتمالاً قدیمی/ناقص» هستند:
- [x] `computeStatsDataFallback` (~خط ۳۹۹۸-۴۰۳۱) و مسیر Web Worker مرتبط (`computeStatsDataViaWorker`/`computeStatsLeadersViaWorker`, خط ۳۹۴۰-۳۹۴۶): اگر هم `InvoiceRepo.getStatsBuckets` و هم Worker شکست بخورند، به‌جای حلقه‌ی JS روی `invoices[]`، یک پیام خطای واضح در UI تب آمار نشان بده («آمار در دسترس نیست — اتصال SQLite را بررسی کنید»). — `computeStatsDataFallback` و `computeStatsLeadersFallback` هر دو کامل حذف شدند (نه فقط حذف فراخوانی؛ خودِ تعریف تابع هم رفت). `fetchStatsData`/`fetchStatsLeaders` حالا وقتی هم Repo و هم Worker شکست بخورند `throw new Error('آمار در دسترس نیست — اتصال SQLite را بررسی کنید')` می‌کنند؛ `renderStatsCharts`/`renderStatsLeaders` این خطا را می‌گیرند و همان پیام را در سه نمودار + سه کارت رکورددار تب آمار نشان می‌دهند (نه کرش، نه صفحه‌ی خالی گمراه‌کننده). یک رخداد مرتبط اضافه هم پیدا و اصلاح شد: `renderStatsBarChart` مستقیماً `invoices.length` را برای تصمیم «نموداری برای نمایش نیست» می‌خواند — این هم به چک روی خودِ داده‌ی bucket ورودی (`data.some(d=>d.count>0)`) تغییر کرد تا هیچ خواندن مستقیمی از آرایه‌ی حافظه در این زنجیره نماند.
- [x] `doBackupLegacyFromMemory` (خط ۶۰۳۴-۶۰۵۴) و فراخوانی‌اش در `catch` بلوک `doBackup` (خط ۶۰۱۲): این مسیر legacy کاملاً حذف شود؛ اگر `buildBackupParts`/`repoForEachBatch` شکست خورد، `doBackup` باید فقط خطا نشان دهد، نه یک بکاپ احتمالاً ناقص از حافظه بسازد (بکاپ ناقص بی‌صدا از عدم بکاپ خطرناک‌تر است). — `doBackupLegacyFromMemory` و helper اختصاصی‌اش `_doDownload` هر دو کامل حذف شدند. `catch` بلوک `doBackup` حالا فقط `backupMsg(false, …)` با متن خطای واقعی + راهنمایی («اتصال SQLite را بررسی کنید و دوباره تلاش کنید») نشان می‌دهد. `_doDownloadBlob` (مسیر موفق اصلی) دست‌نخورده ماند.
- [x] `renderInvoicesList` (خط ۴۷۷۱): خط `let baseList=invoices.slice()` و فیلترهای بعدی‌اش که فقط برای «fallback شمارش وضعیت» (`renderInvStatusCounts`) استفاده می‌شوند — این fallback باید حذف و با پیام خطای واضح در بخش شمارش‌ها جایگزین شود اگر `InvoiceRepo.getStatusCounts` شکست بخورد. — **دامنه‌ی دقیق این مورد در حین اجرا شفاف‌تر شد:** `baseList` علاوه بر fallback شمارش وضعیت، در همین تابع به `renderDebtorSummary` هم پاس داده می‌شود و پایه‌ی ساخت `list`/`ctx.fullList` (رندر اصلی لیست، خط ۴۷۸۴-۴۷۹۹) هم هست — این دو مصرف دیگر صراحتاً فاز ۹ هستند (نگاه کن به آیتم اول فاز ۹ پایین)، نه فاز ۸. پس خودِ خط `baseList=invoices.slice()` عمداً حذف نشد؛ فقط مصرف «fallback شمارش وضعیت» حذف شد: `fetchInvoiceStatusCounts` دیگر پارامتر `baseList` نمی‌گیرد و دیگر هیچ `.forEach` روی آرایه‌ی حافظه ندارد — اگر `InvoiceRepo.getStatusCounts` نبود یا خطا داد، خطا throw می‌کند؛ `renderInvStatusCounts` این را می‌گیرد و روی هر ۴ دکمه‌ی فیلتر (پیش‌فاکتور/نیمه‌تسویه/تسویه‌شده/بدهکار) متن «(خطا)» نشان می‌دهد به‌جای عدد شمارش نادرست/قدیمی.
- **تست‌های خودکار به‌روزرسانی شدند** (این فایل `<script>` خودش تست خودکار ندارد، ولی سه تست `tests/` که مستقیماً `fetchStatsData`/`fetchStatsLeaders`/`STATS_WORKER_SRC` را با regex از html8.html استخراج و اجرا می‌کنند به رفتار جدید (fail-fast) وابسته بودند): `stats-aggregation-test.mjs` (تست‌های ۶–۹، قبلاً fallback را verify می‌کردند، حالا `throw`شدن را verify می‌کنند)، `stats-worker-test.mjs` (تست ۶)، `stats-leaders-test.mjs` (تست‌های ۶–۸) — هر سه اجرا شدند و سبزند (۱۷/۱۸/۱۷ پاس، ۰ خطا). `tests/README.md` هم برای توضیح `stats-worker-test.mjs` به‌روزرسانی شد.
- **رگرسیون سینتکس:** `node memory-array-audit.mjs` بعد از همه‌ی تغییرات بالا اجرا شد — هر سه بلاک `<script>` سالم‌اند.
- **تست دستی روی گوشی هنوز انجام نشده** (طبق هشدار خودِ نقشه‌راه، چون `html8.html` تست خودکار ندارد) — قبل از رفتن سراغ فاز ۹، حتماً روی گوشی امتحان کن: تب آمار (هم حالت عادی، هم با قطع عمدی InvoiceRepo برای دیدن پیام خطا)، دکمه‌ی بکاپ (هم موفق، هم با خطای عمدی)، و فیلترهای وضعیت فاکتور (پیش‌فاکتور/نیمه‌تسویه/تسویه‌شده/بدهکار).

### فاز ۹ — بازنویسی رندر اصلی به کوئری مستقیم SQLite (پرریسک‌ترین فاز)
- [ ] `renderInvoicesList`: بخش ساخت `list`/`ctx.fullList` (خط ۴۷۸۴-۴۷۹۹) که به‌عنوان fallback داخلی `fetchInvoicesPage`/`fetchCustomerInvoicesPage` پاس داده می‌شود، حذف شود. `fetchInvoicesPage`/`fetchCustomerInvoicesPage` باید در نبود Repo فقط خطا برگردانند (fail-fast)، نه به `ctx.fullList` سقوط کنند.
- [ ] `resolveInvoiceById` (خط ۱۰۲۲): مرحله‌ی اول (`invoices.find`) و push انتهایی (`invoices.push`) حذف شود؛ فقط `InvoiceCache` (که خودش از SQLite پر می‌شود، نه آرایه‌ی خام) + `InvoiceRepo.getById` باقی بماند.
- [ ] `addCustomer`/`removeCustomer`/`deleteInvoice`/`saveInvoiceEdit`/`saveCustomerEdit` و توابع مشابه: خطوطی که مستقیم `invoices.push`/`.splice`/`.filter` یا معادل `customers` را می‌زنند (نوشتن‌آینه‌ای محلی بعد از نوشتن واقعی در SQLite، برای «نمایش فوری») حذف شوند؛ به‌جایش بعد از نوشتن موفق در Repo، فقط همان بخش از UI که باید آپدیت شود دوباره از Repo رندر شود (نه از آرایه).

### فاز ۱۰ — بکاپ/ریستور کاملاً بدون آرایه‌ی واسط کامل
- [ ] `doRestoreLegacyJson` (خط ۶۲۲۲): خطوط `invoices=Array.isArray(data.invoices)?...` و مشابه آن برای `customers` که کل فایل ورودی را یک‌جا در آرایه‌ی سراسری می‌ریزند، به یک حلقه‌ی batch-به-batch مستقیم به `InvoiceRepo.bulkInsert`/`CustomerRepo.bulkInsert` تبدیل شود (مثل چیزی که `doRestoreNdjson` با `readBatches` از قبل انجام می‌دهد) — بدون این‌که هیچ‌وقت کل فایل در متغیر سراسری `invoices`/`customers` بماند.
- [ ] خط ۶۲۷۸ (`clearAllData`: `inventory=[];customers=[];invoices=[];...`) اصلاح شود تا فقط `inventory`/`productionQueue`/`vanDrivers` را ریست کند؛ `customers`/`invoices` دیگر باید بعد از حذف فاز ۱۱ اصلاً وجود نداشته باشند.

### فاز ۱۱ — حذف نهایی خودِ متغیرها + رگرسیون کامل
- [ ] بعد از تأیید صفر رخداد باقی‌مانده (طبق اسکریپت فاز ۷)، `customers`/`invoices` از خط تعریف ۶۰۵ (`let inventory=[], customers=[], invoices=[], ...`) حذف شوند.
- [ ] هر سه بلاک `<script>` با `node --check` تأیید شوند.
- [ ] تست دستی کامل روی گوشی (نه فقط syntax check) ضروری است، چون تست‌های خودکار `tests/` این فایل را پوشش نمی‌دهند: افزودن/ویرایش/حذف فاکتور و مشتری، رندر لیست با فیلتر/جستجو/صفحه‌بندی، تب آمار، بکاپ و ریستور (هر دو فرمت)، و رفتار اپ وقتی SQLite/Capacitor واقعاً در دسترس نیست (باید خطای واضح نشان دهد، نه کرش بی‌صدا یا صفحه‌ی سفید).

**ریسک کلی این نقشه‌راه:** بر خلاف idb که یک لایه‌ی کاملاً مجزا بود، این آرایه‌ها با منطق رندر/UI درهم‌تنیده‌اند. توصیه می‌شود بعد از هر فاز (به‌خصوص فاز ۹) قبل از رفتن سراغ فاز بعد، خودت روی گوشی امتحان کنی — نه این‌که همه‌ی فازها را پشت‌سرهم بدون تست میانی انجام بدیم.
