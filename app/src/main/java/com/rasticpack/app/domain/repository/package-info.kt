/**
 * ══ مرحله ۱ — اسکلت لایه‌ی domain ══
 * این پکیج فقط اینترفیس‌های Repository (قرارداد، بدون پیاده‌سازی) نگه می‌دارد — مثلاً
 * `DriverRepository` که فقط امضای توابعی مثل `observeAll()`, `add(...)`, `delete(id)`
 * را تعریف می‌کند. پیاده‌سازی واقعی هرکدام (با Room) در `data/repository/` (طبق ساختار
 * پوشه‌ی هدف در نقشه‌ی معماری) قرار می‌گیرد و همین اینترفیس را implement می‌کند.
 *
 * قانون وابستگی یک‌طرفه: این پکیج فقط از `domain.model` و `core.*` استفاده می‌کند —
 * هرگز از `data.*` (چون خودِ data این پکیج را پیاده‌سازی می‌کند، نه برعکس).
 * اولین اینترفیس (`DriverRepository`، معادل دقیق `data/repo/DriverRepository.kt` فعلی)
 * در مرحله ۲ اضافه می‌شود.
 *
 * این فایل صرفاً برای ثبت پوشه‌ی خالی در Git است؛ هیچ کد اجراشونده‌ای اینجا نیست.
 */
package com.rasticpack.app.domain.repository
